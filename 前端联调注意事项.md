# 前端联调注意事项

## 一、后端基础信息

| 项目 | 值 |
|------|-----|
| 后端地址 | `http://localhost:8080` |
| API 前缀 | `/api/v1` |
| Content-Type | `application/json` |
| 认证方式 | `Authorization: Bearer <token>` |

## 二、测试账号

| 账号 | 密码 | 角色 | 说明 |
|------|------|------|------|
| `admin` | `Admin@2026` | ADMIN | 管理员，可生成激活码、管理用户 |
| `testuser` | `Abc12345` | USER | 需通过注册接口创建 |
| `testvip` | `Abc12345` | VIP | 先注册为 USER，再用激活码升级 |

## 三、CORS 限制

前端必须运行在以下来源之一：

```
http://localhost:*
http://127.0.0.1:*
```

如果使用其他域名（如 `http://192.168.x.x`），需要通知后端在 `WebConfig.java` 中添加。

## 四、新接口说明

### 4.1 VIP 激活

```http
POST /api/v1/user/activate-vip
Authorization: Bearer <token>
Content-Type: application/json

{ "code": "VIP-A1B2C3D4" }
```

**注意：** 激活码需要管理员先生成（见 4.5）。

### 4.2 AI 配置管理

三种模式：

| Provider | 含义 | API Key 来源 | 模型 |
|----------|------|-------------|------|
| `DEFAULT` | 使用服务器默认 DeepSeek Key | 后端 `.env` | `deepseek-chat` |
| `CUSTOM` | 使用用户自己的 API Key | 用户提交 | 用户指定 |
| `LOCAL` | 使用本地 Ollama 模型（需 VIP） | 无需 | 用户指定 |

```http
GET /api/v1/ai-config
Authorization: Bearer <token>
```

```http
POST /api/v1/ai-config
Authorization: Bearer <token>
Content-Type: application/json

{ "provider": "CUSTOM", "apiKey": "sk-xxx", "model": "deepseek-chat" }
```

```http
DELETE /api/v1/ai-config
Authorization: Bearer <token>
```

**注意：**
- `POST` 保存时会自动测试 Key 连通性，DeepSeek 不可达返回 600
- `GET` 返回的 `apiKey` 已脱敏（如 `sk-ab****7890`），仅用于展示
- 非 VIP 用户选 `LOCAL` 返回 400
- `DELETE` 删除后自动回退为 `DEFAULT`

### 4.3 体重趋势

```http
GET /api/v1/weight/trend?period=week
```

| 参数 | 必填 | 默认值 | 说明 |
|------|------|--------|------|
| `period` | 否 | `week` | `week` / `month` / `year` |

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "dates": ["2026-06-01", "2026-06-02"],
    "values": [80.0, 79.5]
  }
}
```

### 4.4 管理员生成激活码

```http
POST /api/v1/admin/vip-codes/generate?count=5
Authorization: Bearer <token>
```

| 参数 | 必填 | 默认值 | 说明 |
|------|------|--------|------|
| `count` | 否 | 1 | 生成数量，1~100 |

**注意：**
- 激活码格式：`VIP-XXXXXXXX`（8 位大写十六进制）
- 有效期：创建后 7 天
- 一次性使用，使用后自动过期

### 4.5 管理员查看激活码列表

```http
GET /api/v1/admin/vip-codes/list
Authorization: Bearer <token>
```

### 4.6 管理员删除体重/计划

```http
DELETE /api/v1/admin/users/weights/{recordId}
DELETE /api/v1/admin/users/plans/{planId}
Authorization: Bearer <token>
```

## 五、接口字段变更

### 5.1 生成次数响应

```http
GET /api/v1/plan/generate-count
```

**字段定稿：**
```json
{
  "used": 1,
  "limit": 3,
  "remaining": 2
}
```

| 字段 | 说明 |
|------|------|
| `used` | 今日已用次数 |
| `limit` | 今日限制次数（普通用户 3，VIP 用户 5） |
| `remaining` | 今日剩余次数 |

### 5.2 健康检查响应

```http
GET /api/v1/health
```

**已移除的字段：** `aiProvider`、`aiService`
**剩余字段：** `mysql`（正常/异常）、`chroma`（正常/异常）

### 5.3 注册密码规则

```http
POST /api/v1/user/register
```

密码需满足：
- 最少 8 位
- 至少包含 1 个大写字母
- 至少包含 1 个小写字母
- 至少包含 1 个数字

**示例：** `Abc12345`

## 六、错误码

| 错误码 | 说明 |
|--------|------|
| 400 | 参数错误 / 密码格式不正确 |
| 401 | 未登录或 Token 无效 / 用户名或密码错误 |
| 403 | 无权限访问 / 账号已禁用 / 每日次数已用完 |
| 404 | 请求的资源不存在 |
| 409 | 数据冲突（用户名重复 / 激活码已使用） |
| 429 | **请求过于频繁**（登录 5 次/分钟，注册 3 次/小时） |
| 500 | 服务器内部异常 |
| 600 | AI 服务调用失败 |
| 601 | Chroma 检索失败 |

## 七、限流说明

| 接口 | 限制 | 窗口 | 超出返回 |
|------|------|------|---------|
| `POST /user/register` | 3 次 | 1 小时 | 429 |
| `POST /user/login` | 5 次 | 1 分钟 | 429 |

限流基于 IP 地址（优先读取 `X-Forwarded-For`，无代理时取 `getRemoteAddr()`）。

## 八、Swagger 文档

```
http://localhost:8080/swagger-ui/index.html
```

需要 admin 账号登录后才能查看。

## 九、注意事项

1. **激活码格式**：`VIP-XXXXXXXX`，8 位大写十六进制，有效期 7 天
2. **AI 配置自动测试**：保存 `CUSTOM` 模式时会调用 DeepSeek 验证 Key，不可达返回 600
3. **API Key 脱敏**：`GET /ai-config` 返回的 Key 已脱敏，仅展示用
4. **Ollama 仅 VIP**：非 VIP 用户选 `LOCAL` 模式返回 400
5. **管理员密码**：`Admin@2026`，生产环境建议第一时间修改
6. **生成次数**：普通用户每天 3 次，VIP 用户每天 5 次
7. **新接口确认**：以下 9 个新接口已就绪——
   - `POST /user/activate-vip`
   - `GET /ai-config`
   - `POST /ai-config`
   - `DELETE /ai-config`
   - `GET /weight/trend`
   - `POST /admin/vip-codes/generate`
   - `GET /admin/vip-codes/list`
   - `DELETE /admin/users/weights/{recordId}`
   - `DELETE /admin/users/plans/{planId}`
