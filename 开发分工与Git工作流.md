# 智能健康助手项目开发分工与 Git 工作流

## 一、协作方式

本项目由两名成员共同开发，采用前后端分离模式。

项目目录规划：

```text
smart-health-frontend   前端项目，Vue3
smart-health-backend    后端项目，Spring Boot
```

分工原则：

- 前端负责人负责页面、路由、接口调用、状态展示和用户交互。
- 后端负责人负责接口、数据库、认证权限、AI 调用、RAG 检索和业务逻辑。
- 前后端通过 RESTful API 和 JSON 数据进行联调。
- 接口字段、路径、请求方式以需求文档和 Swagger/OpenAPI 文档为准。

## 二、前端负责人任务

### 1. 项目基础搭建

任务内容：

- 使用 Vue3 搭建前端项目。
- 配置 Vue Router。
- 配置 Axios 请求封装。
- 引入 Element Plus。
- 配置基础页面布局和导航。

验收标准：

- 前端项目可以正常启动。
- 页面路由可以正常跳转。
- Axios 可统一携带 Token 请求后端接口。
- 接口请求失败时有统一错误提示。

### 2. 用户登录与注册页面

任务内容：

- 实现注册页面。
- 实现登录页面。
- 登录成功后保存 JWT Token。
- 登录后跳转首页。
- 退出登录时清除 Token。

验收标准：

- 用户可完成注册。
- 用户可使用账号密码登录。
- 登录成功后能访问用户端页面。
- 未登录访问受保护页面时跳转登录页。
- 登录失败时显示错误提示。

### 3. GSAP 动画登录界面

任务内容：

- 使用 GSAP 实现健康数据流登录动画。
- 实现登录页表单进入动画。
- 实现健康数据卡片浮现动画。
- 实现心率曲线、体重趋势、数字递增等视觉效果。
- 登录成功后实现登录页到首页的过渡动画。

验收标准：

- 登录页动画流畅，不影响表单输入。
- 点击登录后有 loading 状态。
- 登录成功后有过渡动画并进入首页。
- 动画失败不影响正常登录功能。

### 4. 用户端功能页面

任务内容：

- 首页。
- 健康档案页。
- AI 计划生成页。
- 体重记录页。
- 历史计划页。
- VIP 智能问答页。

验收标准：

- 健康档案可新增、修改、查询。
- 体重记录可添加、查询、删除。
- AI 计划可生成并以卡片形式展示。
- 历史计划可按时间倒序展示。
- 体重趋势可使用折线图展示。
- 普通用户访问智能问答页时显示 VIP 专属提示。
- VIP 用户可看到问答输入框并提交问题。

### 5. 管理员端页面

任务内容：

- 管理员登录页。
- 用户管理页。
- 用户详情页。
- 体重记录管理页。
- AI 计划记录管理页。
- 健康知识库管理页。

验收标准：

- 管理员可登录后台。
- 管理员可查看用户列表和用户详情。
- 管理员可启用、禁用用户。
- 管理员可查看用户体重记录和 AI 计划记录。
- 管理员可新增、修改、删除、启用、禁用健康知识。

### 6. 前端接口联调

任务内容：

- 按后端 Swagger/OpenAPI 文档接入接口。
- 对关键接口响应进行字段校验。
- 对 loading、错误、空数据状态进行处理。

验收标准：

- 所有用户端核心页面能正常调用后端接口。
- 所有管理员端核心页面能正常调用后端接口。
- Token 失效时能跳转登录页。
- 接口异常时页面有明确提示。

## 三、后端负责人任务

### 1. 项目基础搭建

任务内容：

- 使用 Spring Boot 3.2 搭建后端项目。
- 配置 MyBatis。
- 配置 MySQL 数据源。
- 配置 Spring Security。
- 配置 JWT 认证。
- 配置 Swagger/OpenAPI 文档。
- 配置 Flyway 数据库迁移。

验收标准：

- 后端项目可以正常启动。
- MySQL 可以正常连接。
- Swagger 页面可访问。
- Flyway 可自动执行数据库迁移脚本。
- 未登录用户不能访问受保护接口。

### 2. 用户与权限模块

任务内容：

- 实现用户注册。
- 实现用户登录。
- 实现 JWT Token 签发和校验。
- 实现 USER、VIP、ADMIN 角色权限。
- 实现账号启用和禁用。

验收标准：

- 用户密码使用 BCrypt 加密存储。
- 登录成功返回 JWT Token。
- 普通用户只能访问自己的数据。
- VIP 用户可访问 VIP 专属接口。
- 管理员可访问后台接口。
- 禁用用户不能正常登录。

### 3. 健康档案模块

任务内容：

- 实现健康档案新增。
- 实现健康档案修改。
- 实现健康档案查询。
- 实现用户数据归属校验。

验收标准：

- 用户可保存自己的健康档案。
- 用户可修改自己的健康档案。
- 用户不能访问其他用户档案。
- 管理员可查看用户健康档案。

### 4. 体重记录模块

任务内容：

- 实现体重记录新增。
- 实现体重历史查询。
- 实现体重记录删除。
- 实现体重趋势数据查询。

验收标准：

- 用户可添加体重记录。
- 用户可查询自己的体重历史。
- 用户可删除自己的错误记录。
- 用户不能删除其他用户记录。
- 返回数据可支持前端折线图展示。

### 5. AI 计划生成模块

任务内容：

- 实现 AI 计划生成接口。
- 实现普通用户每日 2 次生成限制。
- 实现 VIP 用户每日 5 次生成限制。
- 实现基础计划和 VIP 详细计划区分。
- 实现 DeepSeek API 调用。
- 实现生成结果保存。
- 实现历史计划查询。

验收标准：

- 普通用户每天最多生成 2 次计划。
- VIP 用户每天最多生成 5 次计划。
- 超过次数限制时返回明确提示。
- 普通用户返回基础饮食和运动计划。
- VIP 用户返回热量估算、营养比例、训练强度分级和每周复盘建议。
- 生成结果可保存并在历史记录中查询。

### 6. Chroma RAG 知识库模块

任务内容：

- 封装 ChromaClient。
- 实现健康知识新增、修改、删除、查询。
- 将健康知识 document 和 metadata 直接保存到 Chroma。
- 支持 metadata 字段：category、title、keywords、status、level。
- 实现启用和禁用知识。

验收标准：

- 管理员可维护 Chroma 健康知识。
- 禁用知识不参与检索。
- AI 计划生成时可检索 Top 3 到 Top 5 条知识。
- 普通用户检索 basic 和 all 知识。
- VIP 用户检索 basic、vip 和 all 知识。
- 检索结果可加入 Prompt。

### 7. VIP 智能健康问答模块

任务内容：

- 实现 `POST /chat/ask` 接口。
- 后端校验用户是否为 VIP。
- 使用用户问题检索 Chroma。
- 检索 level 为 vip 或 all 的 enabled 知识。
- 调用 DeepSeek 生成回答。
- 返回回答和参考知识标题。

验收标准：

- 普通用户调用问答接口返回无权限提示。
- VIP 用户可正常提交问题。
- 每次问答检索 Top 3 到 Top 5 条知识。
- 回答末尾包含非医疗建议提示。
- DeepSeek 或 Chroma 调用失败时返回明确错误信息。

### 8. 管理员后台接口

任务内容：

- 查询用户列表。
- 查询用户详情。
- 启用、禁用用户。
- 查询用户体重记录。
- 查询用户 AI 计划记录。
- 删除异常体重或计划记录。

验收标准：

- 非管理员不能访问后台接口。
- 管理员可完成用户管理操作。
- 管理员操作不会越权修改系统敏感配置。
- 删除类操作需要后端校验目标数据存在。

### 9. 测试与文档

任务内容：

- 编写核心 Service 单元测试。
- 编写 Controller 接口测试。
- 编写 Postman 测试集合。
- 维护 Swagger/OpenAPI 文档。
- 编写 Flyway 数据库迁移脚本。

验收标准：

- 核心业务单元测试通过。
- 关键接口 Postman 测试通过。
- Swagger 文档与实际接口一致。
- 数据库表可通过 Flyway 自动创建。

## 四、前后端联调验收标准

### 1. 用户端验收

- 用户可注册、登录、退出。
- 用户可维护健康档案。
- 用户可添加、查看、删除体重记录。
- 用户可生成 AI 饮食和运动计划。
- 用户可查看历史计划。
- 普通用户每天最多生成 2 次计划。
- VIP 用户每天最多生成 5 次计划。
- 普通用户不能使用自由问答。
- VIP 用户可使用智能问答。

### 2. 管理员端验收

- 管理员可登录后台。
- 管理员可查看用户列表和详情。
- 管理员可启用、禁用用户。
- 管理员可查看用户体重记录和计划记录。
- 管理员可管理健康知识库。

### 3. AI 与 RAG 验收

- AI 计划生成前可从 Chroma 检索相关知识。
- 生成计划中能体现用户健康档案和体重趋势。
- VIP 计划内容比普通用户更详细。
- VIP 问答能返回回答和参考知识标题。
- AI 回答包含非医疗建议提示。

### 4. 安全验收

- 密码不明文存储。
- DeepSeek API Key 不出现在前端代码中。
- 普通用户不能访问其他用户数据。
- 普通用户不能调用 VIP 问答接口。
- 非管理员不能访问管理员接口。

## 五、Git 工作流

### 1. 分支规则

主分支：

```text
main
```

开发分支：

```text
dev
```

功能分支：

```text
feature/frontend-login
feature/frontend-profile
feature/frontend-plan
feature/backend-auth
feature/backend-profile
feature/backend-plan
feature/backend-rag
```

修复分支：

```text
fix/问题描述
```

分支要求：

- `main` 只保存稳定版本。
- 日常开发基于 `dev` 拉功能分支。
- 功能完成后提交 Pull Request 合并到 `dev`。
- 阶段验收通过后，再由 `dev` 合并到 `main`。

### 2. 开发流程

```text
拉取最新 dev
 ↓
创建个人功能分支
 ↓
本地开发
 ↓
本地测试
 ↓
提交 commit
 ↓
推送远程分支
 ↓
创建 Pull Request
 ↓
另一名成员 Review
 ↓
修改问题
 ↓
合并到 dev
```

### 3. Commit 规范

使用 Conventional Commits 格式：

```text
type(scope): description
```

常用 type：

- `feat`：新增功能
- `fix`：修复问题
- `docs`：文档修改
- `style`：格式调整
- `refactor`：重构
- `test`：测试相关
- `chore`：构建或配置修改

示例：

```text
feat(frontend): add login page
feat(backend): add jwt authentication
fix(backend): fix user role check
docs: add development workflow
```

### 4. Pull Request 规范

每个 PR 必须包含：

- 变更内容说明。
- 影响范围。
- 测试结果。
- 是否涉及接口变更。
- 是否涉及数据库变更。
- 是否涉及权限逻辑。

PR 合并要求：

- 至少 1 人 Review 通过。
- 本地启动无报错。
- 相关接口或页面自测通过。
- 不提交 `.env`、密钥、Token、数据库密码。
- 不提交无关格式化或无关重构。

### 5. 前后端接口协作规则

- 后端接口变更必须同步更新 Swagger/OpenAPI。
- 前端接入接口前，以 Swagger 文档为准。
- 接口字段使用 camelCase。
- 接口路径统一使用 `/api/v1/` 前缀。
- 如果接口字段、路径或返回结构发生变化，必须提前通知对方。
- 破坏性接口变更必须单独提交 PR。

### 6. 数据库变更规则

- 所有数据库结构变更通过 Flyway 脚本提交。
- 已合并的迁移脚本不能修改。
- 新增字段、修改字段、建表都必须新增迁移文件。
- 涉及删除字段或修改字段类型时，需要双方确认。

### 7. 冲突处理规则

- 谁负责的模块由谁优先解决冲突。
- 前后端公共文档冲突时，双方确认后再合并。
- 不使用强制覆盖方式解决冲突。
- 冲突解决后必须重新运行相关测试。

## 六、阶段交付计划

### 第一阶段：基础框架

前端：

- Vue3 项目搭建
- 路由配置
- Axios 封装
- 登录注册页面初版

后端：

- Spring Boot 项目搭建
- MySQL 连接
- 用户表迁移脚本
- JWT 登录注册接口

验收标准：

- 前端能启动并访问登录页。
- 后端能启动并访问 Swagger。
- 注册登录接口可通过 Postman 测试。

### 第二阶段：核心业务

前端：

- 健康档案页
- 体重记录页
- AI 计划展示页
- 历史计划页

后端：

- 健康档案接口
- 体重记录接口
- AI 计划生成接口
- 历史计划接口

验收标准：

- 用户可以完成健康档案和体重记录操作。
- 用户可以生成并查看 AI 计划。
- 普通用户和 VIP 用户生成次数限制生效。

### 第三阶段：RAG 与 VIP 问答

前端：

- 智能问答页
- 健康知识库管理页
- VIP 权限提示

后端：

- ChromaClient
- RAG 检索
- 健康知识库管理接口
- VIP 问答接口

验收标准：

- 管理员可维护健康知识。
- AI 计划生成可使用 Chroma 检索结果。
- 普通用户不能使用问答。
- VIP 用户可使用 RAG 智能问答。

### 第四阶段：管理员后台与联调

前端：

- 管理员后台页面完善
- 用户管理页面
- 数据展示和异常状态处理

后端：

- 管理员接口完善
- 权限校验完善
- 测试用例补充

验收标准：

- 管理员可完成后台管理操作。
- 普通用户、VIP 用户、管理员权限隔离正确。
- 前后端核心流程联调通过。

### 第五阶段：测试与验收

前端：

- 页面细节优化
- GSAP 动画完善
- 响应式适配

后端：

- 单元测试
- 接口测试
- RAG 测试
- 文档完善

验收标准：

- 核心功能流程完整。
- 关键接口测试通过。
- 页面无明显展示错误。
- 项目可用于演示和答辩。
