server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smart_health?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8mb4
    username: root
    password: 88888888
  flyway:
    enabled: true

logging:
  level:
    com.smarthealth: debug
    org.springframework.security: debug
