<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smarthealth.mapper.VipCodeMapper">

    <resultMap id="codeMap" type="com.smarthealth.entity.VipActivationCode">
        <id property="id" column="id"/>
        <result property="code" column="code"/>
        <result property="status" column="status"/>
        <result property="usedBy" column="used_by"/>
        <result property="createdBy" column="created_by"/>
        <result property="createdAt" column="created_at"/>
        <result property="expiresAt" column="expires_at"/>
        <result property="usedAt" column="used_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO vip_activation_code (code, status, created_by, created_at, expires_at)
        VALUES (#{code}, 0, #{createdBy}, #{createdAt}, #{expiresAt})
    </insert>

    <select id="findByCode" resultMap="codeMap">
        SELECT * FROM vip_activation_code WHERE code = #{code}
    </select>

    <select id="findAll" resultMap="codeMap">
        SELECT * FROM vip_activation_code ORDER BY created_at DESC
    </select>

    <update id="updateStatus">
        UPDATE vip_activation_code
        SET status = #{status}, used_by = #{usedBy}, used_at = #{usedAt}
        WHERE id = #{id}
    </update>
</mapper>
