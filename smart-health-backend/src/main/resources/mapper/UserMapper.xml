<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smarthealth.mapper.UserMapper">

    <resultMap id="userMap" type="com.smarthealth.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="vipExpireTime" column="vip_expire_time"/>
        <result property="lastActiveTime" column="last_active_time"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="findById" resultMap="userMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="findByUsername" resultMap="userMap">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="findAll" resultMap="userMap">
        SELECT * FROM user ORDER BY create_time DESC
    </select>

    <select id="findByPage" resultMap="userMap">
        SELECT * FROM user
        <where>
            <if test="keyword != null and keyword != ''">
                username LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByKeyword" resultType="long">
        SELECT COUNT(*) FROM user
        <where>
            <if test="keyword != null and keyword != ''">
                username LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM user
    </select>

    <select id="countByRole" resultType="long">
        SELECT COUNT(*) FROM user WHERE role = #{role}
    </select>

    <select id="countByStatus" resultType="long">
        SELECT COUNT(*) FROM user WHERE status = #{status}
    </select>

    <select id="countCreatedSince" resultType="long">
        SELECT COUNT(*) FROM user WHERE create_time &gt;= #{startTime}
    </select>

    <select id="countActiveSince" resultType="long">
        SELECT COUNT(*) FROM user WHERE status = 1 AND last_active_time &gt;= #{startTime}
    </select>

    <select id="findRecentActive" resultType="com.smarthealth.dto.response.DashboardRecentUserResponse">
        SELECT
            id,
            username,
            role,
            status,
            CASE WHEN last_active_time &gt;= #{onlineStart} THEN TRUE ELSE FALSE END AS online,
            last_active_time AS lastActiveTime
        FROM user
        WHERE last_active_time IS NOT NULL
        ORDER BY last_active_time DESC
        LIMIT #{limit}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, role, status)
        VALUES (#{username}, #{password}, #{role}, #{status})
    </insert>

    <update id="updateStatus">
        UPDATE user SET status = #{status} WHERE id = #{id}
    </update>

    <update id="updateLastActiveTime">
        UPDATE user SET last_active_time = #{lastActiveTime} WHERE id = #{id}
    </update>

    <update id="updateRoleAndVipExpire">
        UPDATE user SET role = #{role}, vip_expire_time = #{vipExpireTime} WHERE id = #{id}
    </update>
</mapper>
