# 智能健康助手——个性化饮食与运动规划系统需求文档（已完善 AI 辅助开发与团队协作标准）

## 一、需求合理性判断

本项目需求合理，适合作为 Vue3 + Spring Boot + MySQL + DeepSeek API 的前后端分离 Web 项目。

核心功能包括用户认证、健康档案、体重记录、AI 饮食计划、AI 运动计划、历史记录和管理员管理，功能边界清晰，开发难度适中。

需要注意：

- AI 生成内容仅作为健康管理参考，不能作为医疗诊断或治疗建议。
- DeepSeek API Key 必须保存在后端，不能暴露到前端。
- 用户密码必须加密存储。
- AI 返回内容建议使用结构化 JSON，方便前端展示。
- 普通用户、管理员、VIP 用户需要通过角色字段进行权限区分。
- 普通用户每天最多生成 2 次 AI 计划，VIP 用户每天最多生成 5 次 AI 计划。

## 二、项目概述

### 1. 项目名称

智能健康助手——个性化饮食与运动规划系统

### 2. 项目目标

开发一个基于 Web 的智能健康管理平台，实现用户健康档案管理、体重记录、AI 个性化饮食与运动计划生成、历史记录查询和后台管理。

### 3. 技术选型

前端：

- Vue3
- Vue Router
- Axios
- Element Plus

后端：

- Spring Boot 3.2
- Spring MVC
- MyBatis
- JWT+spring security

数据库：

- MySQL 8.0

AI 服务：

- DeepSeek API

RAG 检索增强：

- Chroma 向量数据库

## 三、系统架构

系统采用前后端分离架构。

前端项目和后端项目独立开发、独立运行、独立部署：

```text
smart-health-frontend   Vue3 前端项目
smart-health-backend    Spring Boot 后端项目
```

系统调用关系：

```text
用户浏览器
 ↓
Vue3 前端
 ↓ Axios
Spring Boot 后端
 ↓
MySQL 数据库

Spring Boot 后端
 ↓
DeepSeek API

Spring Boot 后端
 ↓
Chroma 向量数据库
 ↓
健康知识库
```

前端只负责页面展示、表单提交和接口调用；后端负责用户认证、权限校验、业务处理、数据库操作、Chroma 检索和 AI 接口调用。

## 四、系统角色

### 1. 普通用户

普通用户可以注册、登录系统，维护个人健康档案，记录体重，生成并查看自己的饮食和运动计划。

权限：

- 注册、登录、退出
- 管理个人健康档案
- 添加、查询、删除自己的体重记录
- 生成 AI 饮食和运动计划
- 查询自己的历史计划

### 2. 管理员

管理员用于后台管理系统数据和用户信息。

权限：

- 登录后台
- 查看用户列表
- 查看用户健康档案
- 查看用户体重记录
- 查看用户 AI 计划记录
- 禁用或启用用户账号
- 删除违规或异常数据
- 管理健康知识库

### 3. VIP 用户

VIP 用户拥有比普通用户更高的 AI 计划生成次数和更详细的计划内容。

权限：

- 每天最多生成 5 次 AI 计划。
- 生成详细饮食和运动计划。
- 查看热量估算。
- 查看营养比例建议。
- 查看训练强度分级。
- 查看每周复盘建议。
- 使用 VIP 专属智能健康问答功能。
- 可通过前端升级 VIP 页面了解普通用户和 VIP 用户的功能差异。

## 五、功能需求

### 1. 用户管理模块

#### 1.1 用户注册

- 用户输入用户名和密码完成注册。
- 用户名不能为空且不能重复。
- 密码不能为空，并在后端加密存储。
- 注册成功后返回成功提示。

#### 1.2 用户登录

- 用户输入用户名和密码登录。
- 登录成功后返回 JWT Token。
- 登录失败返回错误提示。
- 前端后续请求携带 Token。

#### 1.3 用户退出

- 前端清除本地 Token。
- 跳转到登录页面。

#### 1.4 角色权限

- 用户表保存角色字段。
- 普通用户只能访问自己的数据。
- 管理员可以访问后台管理接口。
- 普通用户每天最多生成 2 次 AI 计划。
- VIP 用户每天最多生成 5 次 AI 计划。
- 普通用户不开放自由问答功能。
- VIP 用户开放自由问答功能。
- 后端根据 JWT 中的用户身份和角色进行权限校验。

### 2. 健康档案管理模块

健康档案字段：

- 年龄
- 性别
- 身高
- 当前体重
- 活动水平
- 饮食偏好
- 健康目标

功能：

- 新增健康档案
- 修改健康档案
- 查询健康档案

权限：

- 普通用户只能管理自己的健康档案。
- 管理员可以查看用户健康档案。

### 3. AI 计划生成模块

系统根据用户健康档案和历史体重变化生成个性化计划。

系统在调用 DeepSeek API 前，需要先通过 Chroma 向量数据库检索健康知识库内容，并将检索结果加入 Prompt，实现 RAG 检索增强生成。

生成内容：

- 一周饮食计划
- 一周运动计划
- 调整建议
- 健康提示

生成次数限制：

- 普通用户每天最多生成 2 次 AI 计划。
- VIP 用户每天最多生成 5 次 AI 计划。
- 超过次数限制后，系统提示当日生成次数已用完。

内容差异：

- 普通用户生成基础饮食和运动计划。
- VIP 用户生成详细饮食和运动计划，包括热量估算、营养比例、训练强度分级和每周复盘建议。

饮食计划内容：

- 早餐
- 午餐
- 晚餐
- 加餐建议

运动计划内容：

- 训练类型
- 训练时长
- 训练频率
- 运动强度

VIP 额外内容：

- 每餐热量估算
- 蛋白质、碳水、脂肪比例建议
- 运动训练强度分级
- 每周执行情况复盘建议

RAG 检索要求：

- 健康知识内容和 metadata 直接存储在 Chroma 中。
- 后端根据用户健康目标、饮食偏好、活动水平和体重趋势构造检索 query。
- 每次生成计划时，从 Chroma 检索 Top 3 到 Top 5 条相关健康知识。
- 普通用户检索 basic 和 all 级别知识。
- VIP 用户检索 basic、vip 和 all 级别知识。
- 检索结果需要加入 Prompt，再调用 DeepSeek API 生成计划。
- 禁用状态的知识不参与检索。

Prompt 输入信息：

- 年龄
- 性别
- 身高
- 当前体重
- 活动水平
- 饮食偏好
- 健康目标
- 最近体重变化趋势

生成结果需要保存到数据库，用户可在历史计划中查看。

### 4. 体重记录模块

体重记录字段：

- 日期
- 体重值

功能：

- 添加体重记录
- 查询体重历史
- 删除错误记录
- 展示体重变化趋势

权限：

- 普通用户只能操作自己的体重记录。
- 管理员可以查看和删除异常体重记录。

### 5. AI 记忆优化模块

系统读取用户历史体重记录，分析体重变化趋势，并将趋势加入下一次 AI 计划生成的 Prompt。

趋势规则：

- 体重持续下降：保持当前计划强度。
- 体重无明显变化：调整饮食结构，提高运动强度。
- 体重上升：减少高热量饮食建议，增加运动频率。

功能：

- 读取最近体重记录
- 分析体重趋势
- 生成趋势描述
- 将趋势描述加入 Prompt
- 生成新的饮食和运动计划

### 6. 历史记录模块

功能：

- 查询历史饮食计划
- 查询历史运动计划
- 查询历史体重记录
- 查看计划详情

展示要求：

- 历史计划按创建时间倒序展示。
- 体重记录按日期排序展示。

权限：

- 普通用户只能查看自己的历史记录。
- 管理员可以查看用户历史记录。

### 7. 管理员后台模块

功能：

- 管理员登录
- 查看用户列表
- 查询用户详情
- 查看用户健康档案
- 查看用户体重记录
- 查看用户 AI 计划
- 启用或禁用用户账号
- 删除异常体重记录或计划记录
- 新增健康知识
- 修改健康知识
- 删除健康知识
- 启用或禁用健康知识
- 按分类或关键词查询健康知识

### 8. RAG 知识库模块

系统使用 Chroma 保存健康知识内容和 metadata，不在 MySQL 中单独保存知识库表。

Chroma document 内容：

- 健康知识正文

Chroma metadata 字段：

- category：知识分类
- title：知识标题
- keywords：关键词
- status：状态，enabled 或 disabled
- level：适用级别，basic、vip 或 all

知识分类示例：

- 减脂饮食
- 增肌饮食
- 控糖饮食
- 低盐饮食
- 有氧运动
- 力量训练
- BMI 分类
- 体重管理
- 运动注意事项

RAG 生成流程：

```text
用户点击生成 AI 计划
 ↓
后端读取健康档案
 ↓
后端读取体重记录并分析趋势
 ↓
后端构造 Chroma 检索 query
 ↓
Chroma 返回相关健康知识
 ↓
后端拼接增强 Prompt
 ↓
调用 DeepSeek API
 ↓
保存 AI 计划
 ↓
返回前端展示
```

### 9. VIP 智能健康问答模块

VIP 智能健康问答模块支持用户自由输入健康、饮食、运动相关问题。系统先从 Chroma 健康知识库中检索相关知识，再调用 DeepSeek API 生成回答。

权限要求：

- 普通用户不开放自由问答功能。
- VIP 用户可以使用自由问答功能。
- 未登录用户不能使用问答功能。
- 后端接口必须校验用户是否为 VIP。

问答流程：

```text
VIP 用户输入问题
 ↓
前端提交问题
 ↓
后端校验 VIP 权限
 ↓
后端将问题作为检索 query
 ↓
Chroma 检索相关健康知识
 ↓
获取 Top 3 到 Top 5 条知识
 ↓
拼接 Prompt
 ↓
调用 DeepSeek API
 ↓
返回回答和参考知识标题
 ↓
前端展示问答结果
```

Chroma 检索要求：

- 仅检索 status 为 enabled 的知识。
- 检索 level 为 vip 或 all 的知识。
- 每次问答检索 Top 3 到 Top 5 条相关知识。
- 检索结果加入 Prompt 后再调用 DeepSeek API。

回答要求：

- 回答清晰、简洁、可执行。
- 优先参考检索到的健康知识。
- 不提供疾病诊断或治疗结论。
- 涉及疾病、疼痛、药物或严重不适时，提醒用户咨询医生。
- 回答末尾提示“本回答仅供健康管理参考，不能替代专业医疗建议。”

当前版本不保存聊天记录，不做多轮上下文记忆。

### 10. VIP 升级说明页面

VIP 升级说明页面用于向普通用户展示 VIP 能力差异，引导用户了解升级后可获得的功能。

页面内容：

- 普通用户和 VIP 用户能力对比。
- 普通用户每日 2 次 AI 计划生成次数说明。
- VIP 用户每日 5 次 AI 计划生成次数说明。
- VIP 详细计划能力说明，包括热量估算、营养比例、训练强度分级和每周复盘建议。
- VIP 智能健康问答能力说明。

当前版本该页面仅作为前端能力说明和升级入口展示，不接入支付、订单、续期或真实开通流程。

## 六、数据库设计

### 1. 用户表 user

| 字段名             | 类型           | 说明                |
| --------------- | ------------ | ----------------- |
| id              | bigint       | 主键                |
| username        | varchar(50)  | 用户名               |
| password        | varchar(255) | 加密后的密码            |
| role            | varchar(20)  | 角色：USER、ADMIN、VIP |
| status          | int          | 状态：1 正常，0 禁用      |
| vip_expire_time | datetime     | VIP 到期时间，可为空      |
| create_time     | datetime     | 创建时间              |
| update_time     | datetime     | 更新时间              |

### 2. 健康档案表 health_profile

| 字段名             | 类型           | 说明         |
| --------------- | ------------ | ---------- |
| id              | bigint       | 主键         |
| user_id         | bigint       | 用户 ID      |
| age             | int          | 年龄         |
| gender          | varchar(20)  | 性别         |
| height          | decimal(5,2) | 身高，单位 cm   |
| weight          | decimal(5,2) | 当前体重，单位 kg |
| activity_level  | varchar(50)  | 活动水平       |
| diet_preference | varchar(255) | 饮食偏好       |
| goal            | varchar(100) | 健康目标       |
| create_time     | datetime     | 创建时间       |
| update_time     | datetime     | 更新时间       |

### 3. 体重记录表 weight_record

| 字段名         | 类型           | 说明        |
| ----------- | ------------ | --------- |
| id          | bigint       | 主键        |
| user_id     | bigint       | 用户 ID     |
| weight      | decimal(5,2) | 体重值，单位 kg |
| record_date | date         | 记录日期      |
| create_time | datetime     | 创建时间      |

### 4. AI 计划表 plan

| 字段名           | 类型           | 说明                          |
| ------------- | ------------ | --------------------------- |
| id            | bigint       | 主键                          |
| user_id       | bigint       | 用户 ID                       |
| plan_type     | varchar(30)  | 计划类型：DIET、EXERCISE、COMBINED |
| plan_level    | varchar(20)  | 计划级别：BASIC、VIP              |
| plan_content  | text         | 计划内容                        |
| trend_summary | varchar(500) | 体重趋势分析                      |
| create_time   | datetime     | 创建时间                        |

### 5. AI 计划生成记录表 plan_generate_record

| 字段名            | 类型       | 说明     |
| -------------- | -------- | ------ |
| id             | bigint   | 主键     |
| user_id        | bigint   | 用户 ID  |
| generate_date  | date     | 生成日期   |
| generate_count | int      | 当日生成次数 |
| create_time    | datetime | 创建时间   |
| update_time    | datetime | 更新时间   |

## 七、接口设计

### 1. 用户接口

| 功能     | 方法   | 路径             |
| ------ | ---- | -------------- |
| 用户注册   | POST | /user/register |
| 用户登录   | POST | /user/login    |
| 当前用户信息 | GET  | /user/current  |

### 2. 健康档案接口

| 功能     | 方法   | 路径       |
| ------ | ---- | -------- |
| 查询健康档案 | GET  | /profile |
| 保存健康档案 | POST | /profile |
| 修改健康档案 | PUT  | /profile |

### 3. AI 计划接口

| 功能       | 方法   | 路径                   |
| -------- | ---- | -------------------- |
| 生成 AI 计划 | POST | /plan/generate       |
| 查询历史计划   | GET  | /plan/history        |
| 查询计划详情   | GET  | /plan/{id}           |
| 查询今日生成次数 | GET  | /plan/generate-count |

### 4. 体重记录接口

| 功能     | 方法     | 路径              |
| ------ | ------ | --------------- |
| 添加体重记录 | POST   | /weight/add     |
| 查询体重历史 | GET    | /weight/history |
| 删除体重记录 | DELETE | /weight/{id}    |

### 5. 管理员接口

| 功能       | 方法  | 路径                        |
| -------- | --- | ------------------------- |
| 查询用户列表   | GET | /admin/users              |
| 查询用户详情   | GET | /admin/users/{id}         |
| 启用用户     | PUT | /admin/users/{id}/enable  |
| 禁用用户     | PUT | /admin/users/{id}/disable |
| 查询用户体重记录 | GET | /admin/users/{id}/weights |
| 查询用户计划记录 | GET | /admin/users/{id}/plans   |

### 6. 知识库接口

| 功能       | 方法     | 路径                            |
| -------- | ------ | ----------------------------- |
| 新增健康知识   | POST   | /admin/knowledge              |
| 修改健康知识   | PUT    | /admin/knowledge/{id}         |
| 删除健康知识   | DELETE | /admin/knowledge/{id}         |
| 查询健康知识列表 | GET    | /admin/knowledge/list         |
| 启用健康知识   | PUT    | /admin/knowledge/{id}/enable  |
| 禁用健康知识   | PUT    | /admin/knowledge/{id}/disable |

### 7. 智能问答接口

| 功能       | 方法   | 路径        |
| -------- | ---- | --------- |
| VIP 智能问答 | POST | /chat/ask |

请求参数：

```json
{
  "question": "减脂期间晚餐怎么吃？"
}
```

返回结果：

```json
{
  "answer": "减脂期间晚餐建议控制总热量，增加优质蛋白和蔬菜摄入，减少高油高糖食物。本回答仅供健康管理参考，不能替代专业医疗建议。",
  "references": [
    {
      "title": "减脂期间饮食原则",
      "category": "减脂饮食"
    }
  ]
}
```

权限说明：

- 仅 VIP 用户可调用。
- 普通用户调用时返回无权限提示。

## 八、页面设计

### 1. 用户端页面

- 登录页
- 注册页
- 首页
- 健康档案页
- AI 计划生成页
- 体重记录页
- 历史计划页
- 智能问答页
- 升级 VIP 页

### 2. GSAP 登录动画设计

用户登录页采用“健康数据流”动画方案，使用 GSAP 实现页面进入动画、数据面板动画和登录成功后的过渡动画。

登录页布局：

- 左侧为登录表单。
- 右侧为健康数据动态面板。
- 背景使用简洁的健康科技风格。

登录前动画：

- 页面标题、输入框、按钮依次进入。
- 健康数据卡片依次浮现。
- 心率曲线从左到右绘制。
- 体重趋势折线动态展示。
- 热量、运动完成率等数字从 0 递增到目标值。

登录按钮点击后动画：

- 登录按钮进入加载状态。
- 登录表单淡出。
- 健康数据卡片向页面中心聚合。
- 页面显示“正在读取健康档案”“正在同步体重趋势”“正在准备个性化计划”等状态提示。
- 登录成功后，健康数据核心展开并过渡到首页。

首页进入动画：

- 首页欢迎语淡入。
- 今日概览卡片从中心展开。
- 体重趋势图从左到右绘制。
- AI 计划入口短暂高亮。
- 健康档案、体重记录、历史计划等模块依次出现。

### 3. 管理员端页面

- 管理员登录页
- 用户管理页
- 用户详情页
- 体重记录管理页
- AI 计划记录管理页
- 健康知识库管理页

### 4. 智能问答页面

智能问答页为 VIP 专属页面。

VIP 用户页面内容：

- 聊天记录展示区域
- 问题输入框
- 发送按钮
- AI 回答加载状态
- 本次参考知识标题

普通用户页面内容：

- 显示 VIP 权限提示。
- 不显示自由问答输入框。

页面提示文案：

```text
智能健康问答为 VIP 专属功能，开通 VIP 后可使用。
```

## 九、核心业务流程

### 1. 普通用户流程

```text
注册账号
 ↓
登录系统
 ↓
填写健康档案
 ↓
添加体重记录
 ↓
生成 AI 计划
 ↓
查看饮食和运动建议
 ↓
持续记录体重
 ↓
系统根据趋势优化下一次计划
```

### 2. 管理员流程

```text
管理员登录
 ↓
进入后台
 ↓
查看用户列表
 ↓
查看用户健康档案、体重记录和计划记录
 ↓
处理异常账号或异常数据
```

### 3. AI 计划生成流程

```text
用户点击生成计划
 ↓
后端读取健康档案
 ↓
后端读取体重记录
 ↓
分析体重趋势
 ↓
构造 Prompt
 ↓
调用 DeepSeek API
 ↓
保存生成结果
 ↓
返回前端展示
```

## 十、非功能需求

### 1. 安全要求

- 密码使用 BCrypt 加密。
- 接口使用 JWT 认证。
- 后端校验用户角色和数据归属。
- DeepSeek API Key 只保存在后端配置中。
- 日志中不输出密码、Token 和 API Key。

### 2. 前后端分离要求

- 前端项目和后端项目分开存放。
- 前端通过 Axios 调用后端接口。
- 后端只提供 RESTful API，不直接返回页面。
- 前后端通过 JSON 交换数据。

### 3. 展示要求

- AI 计划以卡片形式展示。
- 体重变化使用折线图展示。
- AI 生成过程中显示加载状态。
- 接口失败时显示错误提示。
- 登录页使用 GSAP 实现健康数据流动画。
- 登录成功后通过 GSAP 实现从登录页到首页的过渡动画。
- 动画不影响登录、注册和接口请求等核心功能。
- AI 计划详情页可展示本次参考的健康知识标题。
- VIP 智能问答页以聊天窗口形式展示问题和回答。
- 普通用户访问智能问答页时显示 VIP 专属功能提示。

### 4. 前后端接口一致性要求（新增）

前后端接口一致性是项目质量的基石，必须通过契约、校验和版本管理三道防线保障。

**OpenAPI/Swagger 契约**
所有后端 REST 接口必须使用 SpringDoc OpenAPI 3.0 注解（`@Operation`、`@Schema`），自动生成 OpenAPI 规范文档，访问路径为 `/swagger-ui/index.html`。CI 流程中需使用 `spectral` 对生成的 `openapi.json` 进行 lint 校验，确保文档与实现一致。

**JSON Schema 校验**
后端 DTO 必须使用 `@JsonProperty` 显式声明字段名（camelCase），保证前后端字段名完全一致。基于 OpenAPI 文档自动生成 JSON Schema 文件，前端使用 `ajv` 对关键接口（如 AI 计划生成、智能问答）的响应进行 Schema 校验，拦截字段缺失或类型错误。

**Mock 数据与并行开发**
前端在开发阶段可使用 Swagger Mock 或 Postman Mock Server 提供模拟数据，不依赖后端环境。`@ApiResponse` 注解中必须提供示例响应体，供 Mock 和文档同时使用。

**DTO 规范示例**

```java
public record ChatResponse(
    @JsonProperty("sessionId") String sessionId,
    @JsonProperty("answer") @Schema(example = "RAG 是检索增强生成技术...") String answer,
    @JsonProperty("references") List<Reference> references,
    @JsonProperty("createdAt") @Schema(example = "2026-06-07T12:00:00Z") Instant createdAt
) {}
```

### 5. 版本管理要求（新增）

**API 版本**
接口路径统一使用 URL 版本前缀：`/api/v1/`。当需要不兼容变更（删除字段、修改字段类型、改变业务含义）时，升级为 `/api/v2/`，旧版本至少保留一个发布周期。新增可选字段、新增接口不属于破坏性变更，可在当前版本内扩展。

**数据库迁移版本**
数据库变更统一使用 Flyway 管理，迁移脚本放置于 `src/main/resources/db/migration`，命名规则为 `V{版本号}__{描述}.sql`。已应用的迁移不可修改，仅通过新增脚本进行回滚或调整。破坏性变更（DROP COLUMN、ALTER COLUMN 类型）必须在团队评审通过后单独编写迁移脚本并记录影响范围。

**依赖版本**
在 `pom.xml` 中使用 `<properties>` 统一管理 Spring Boot、MyBatis、Chroma Client 等关键依赖版本号，并通过 `maven-enforcer-plugin` 强制最低 Java 17 和 Maven 3.8 版本。

**AI 模型版本**
DeepSeek 模型名称通过配置文件指定，切换模型不影响业务代码逻辑。每次模型升级需在测试集上重新评估验收指标。

### 6. RAG 系统验收标准（新增）

RAG 系统在上线前必须通过标准化验收，使用 20 个问答对进行评估，覆盖事实性、推理性和边界问题。

**测试集组成**

- 事实性问题（答案可直接从健康知识库找到）：12 个
- 推理性问题（需组合多个知识片段）：5 个
- 边界问题（知识库中不存在的信息，如“XX 新药能减肥吗”）：3 个

**验收指标**
| 指标  | 最低要求 | 计算方法 |
| --- | --- | --- |
| 正确率 | ≥80% | 回答覆盖 `must_contain` 关键词且源自检索片段 / 总问题数 |
| 单次延迟 | ≤3 秒 | 从检索开始到 DeepSeek 返回完整答案的时间 |
| 幻觉率 | ≤5% | 回答内容不在 `retrieved_chunks` 中的句子数 / 总问题数 |

验收脚本 `evaluate_rag.py` 须在 CI 流程中可重复执行，测试结果作为上线审批依据。

### 7. 测试策略（新增）

**自动化测试分层**

- 单元测试：Service 层和工具类，使用 JUnit 5 + Mockito，覆盖率不低于 70%。
- 集成测试：Controller 层使用 `@SpringBootTest` 和 MockMvc 验证接口行为。
- 契约测试：对 AI 计划生成和智能问答接口编写 Pact 或 Spring Cloud Contract 契约，CI 中验证。

**Postman 集合与自动化**
为所有关键接口编写 Postman 测试集合，覆盖正常、异常和边界场景（如无权限、超次数、参数缺失）。使用 Newman CLI 在 CI 流水线中执行测试，并导出 JSON 报告。AI 可辅助生成 Pre-request Script 和 Tests 断言脚本，但断言逻辑需人工确认。

### 8. AI 辅助开发使用边界（新增）

明确团队成员使用 AI 辅助开发的边界，提升效率的同时保障代码安全和可维护性。

**AI 可生成的代码**
| 类别 | 说明 |
| --- | --- |
| 单元测试 | 测试类、Mock 设置、断言（使用 Mockito + JUnit 5） |
| Controller / DTO | 标准 CRUD 接口、请求/响应类 |
| MyBatis 映射文件 | 简单 SQL 的 XML 映射 |
| 注释 | 方法注释、类注释、字段说明 |
| commit message | 符合 Conventional Commits 格式 |

**AI 不可生成的代码**
| 类别 | 原因 |
| --- | --- |
| 事务边界（@Transactional） | 影响数据一致性，需人工确定回滚策略 |
| 权限校验（@PreAuthorize / Security 配置） | 涉及角色和数据归属的安全敏感性 |
| 资金计算逻辑 | 精度和一致性要求极高 |
| 数据库索引设计 | 依赖实际查询负载和性能测试 |
| 敏感配置（API Key、加密密钥） | 绝对不能由 AI 建议或生成 |

**标注规范**
所有 AI 生成的代码块必须在注释第一行标注 `// @generated by AI`，在 PR 中由 Reviewer 确认后标记 `[AI Reviewed]`。未标注的 AI 生成代码不得合入主分支。

## 十一、团队 AI 协作规范（新增）

### 1. AI 辅助 Git 工作流

**Commit Message 生成**
提交代码时，可借助 AI 根据 `git diff` 输出生成 Conventional Commits 格式的 commit message。生成的 message 必须人工确认并修改后使用，禁止直接推送未审查的 message。

**PR 描述自动填充**
PR 模板需包含 “变更摘要”“测试结果”“AI 辅助说明”等章节。CI 中可集成 AI 自动根据 diff 生成变更摘要和风险提示，但 Reviewer 仍需逐条审核并填写“人工审核重点”。

**冲突处理建议**
当发生合并冲突时，AI 可对冲突双方的变更意图进行分析并给出合并建议，但最终解决由最熟悉代码的开发者完成。AI 绝不可直接修改冲突文件。

**Code Review 辅助**
AI 自动对 PR 中的代码进行静态分析，在可疑位置（空指针风险、未处理异常、缺少 `@JsonProperty` 等）添加 Review 评论。所有 AI 评论须由人工 Reviewer 确认是否采纳。

### 2. AI 辅助开发工作流

**每日站会准备**
AI 可每日自动汇总：已合并 PR 列表及变更摘要、未处理的 Code Review 评论、最新测试执行结果、今日建议的优先级任务。团队成员基于摘要快速对齐进度。

**调试与文档**
鼓励使用 AI 辅助异常堆栈分析、API 文档生成和单元测试编写，但任何给出的修复方案必须在本地验证通过后才能提交。

### 3. AI 使用约束

- 禁止将生产环境 API Key 或用户数据输入公共 AI 服务。
- 用于开发辅助的 AI 提示词中不得包含真实用户密码、Token 或个人身份信息。
- AI 生成的健康建议类示例数据仅用于测试，必须标识为虚拟数据。

## 十二、预期成果

完成一个前后端分离的智能健康管理系统，实现：

- 普通用户注册登录
- VIP 用户权限区分
- 管理员后台管理
- GSAP 动画登录界面
- 健康档案管理
- 体重记录和趋势分析
- AI 饮食计划生成
- AI 运动计划生成
- Chroma RAG 检索增强生成
- VIP 专属 RAG 智能健康问答
- 历史计划查询
- 基于体重趋势的计划优化
- 符合 OpenAPI 3.0 规范且可校验的接口
- Flyway 管理的数据库版本
- 达到验收指标的 RAG 质量
- 完整的分层自动化测试
- 遵循 AI 辅助开发边界的代码与协作流程

## 十三、风险与处理

| 风险                  | 处理方式                       |
| ------------------- | -------------------------- |
| AI 返回格式不稳定          | 要求返回固定 JSON 格式             |
| DeepSeek API 调用失败   | 显示失败提示，允许重新生成              |
| Chroma 检索失败         | 提示检索失败，允许重新生成              |
| 知识库内容质量较低           | 管理员维护和禁用不合适知识              |
| 用户越权访问数据            | 后端校验 JWT 和用户 ID            |
| 普通用户越权调用问答接口        | 后端校验 VIP 角色并返回无权限提示        |
| 管理员权限滥用             | 管理员接口单独鉴权                  |
| 健康建议被误解             | 页面显示非医疗建议提示                |
| API 不兼容变更导致前端异常     | 使用版本化路径、CI 中运行契约测试         |
| AI 生成代码引入安全漏洞       | 所有 AI 代码必须人工审查并标注          |
| Chroma 向量数据版本与代码不一致 | 迁移数据时使用版本号标记，并纳入 Flyway 管理 |
