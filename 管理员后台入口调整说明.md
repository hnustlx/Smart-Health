# 管理员后台入口调整说明

## 调整目标

管理员后台改为通过独立 URL 进入，并使用独立登录页面和后台布局，避免与普通用户端页面、导航栏和会员信息混在一起。

## 前端路由调整

新增管理员登录入口：

```text
/admin/login
```

新增管理员后台路由：

```text
/admin
/admin/users
/admin/users/:id
/admin/users/:id/weights
/admin/users/:id/plans
/admin/knowledge
```

访问规则：

- 未登录访问 `/admin` 或 `/admin/*` 时，跳转到 `/admin/login`。
- 非管理员账号访问 `/admin` 或 `/admin/*` 时，提示无权限并跳转到 `/admin/login`。
- 管理员登录成功后进入 `/admin`。
- 管理员退出后台后返回 `/admin/login`。
- 普通用户端左侧导航不再显示“管理员后台”入口。

## 页面与布局调整

新增管理员专用布局：

```text
smart-health-frontend/src/layouts/AdminLayout.vue
```

新增管理员页面：

```text
smart-health-frontend/src/views/admin/AdminLoginView.vue
smart-health-frontend/src/views/admin/AdminDashboardView.vue
smart-health-frontend/src/views/admin/AdminUsersView.vue
smart-health-frontend/src/views/admin/AdminUserDetailView.vue
smart-health-frontend/src/views/admin/AdminUserWeightsView.vue
smart-health-frontend/src/views/admin/AdminUserPlansView.vue
smart-health-frontend/src/views/admin/AdminKnowledgeView.vue
```

## 接口变更

本次没有新增后端接口。

管理员登录仍复用现有登录接口：

```text
POST /user/login
```

前端在登录成功后校验返回用户角色是否为 `ADMIN`。如果不是管理员账号，则清除登录状态并提示无管理员权限。

后台管理接口仍使用需求文档中已有接口：

```text
GET /admin/users
GET /admin/users/{id}
PUT /admin/users/{id}/enable
PUT /admin/users/{id}/disable
GET /admin/users/{id}/weights
GET /admin/users/{id}/plans
POST /admin/knowledge
PUT /admin/knowledge/{id}
DELETE /admin/knowledge/{id}
GET /admin/knowledge/list
PUT /admin/knowledge/{id}/enable
PUT /admin/knowledge/{id}/disable
```

## 数据库变更

本次没有数据库变更。

没有新增表、字段或迁移脚本。

## 需求文档影响

需求文档中已经包含“管理员登录页”和管理员后台功能，但没有明确写出独立 URL。

建议在需求文档“管理员端页面”或“管理员流程”中补充：

```text
管理员后台使用独立入口 /admin/login 登录。
管理员登录成功后进入 /admin 后台管理区域。
管理员后台与普通用户端页面布局分离。
```

