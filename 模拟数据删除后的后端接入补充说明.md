# 模拟数据删除后的后端接入补充说明

## 1. 结论

当前前端新增的页面样式、按钮样式、表格筛选、健康画像完成态、普通用户和 VIP 用户展示分层逻辑，删除模拟数据后仍会保留。

但以下能力不是纯前端能力，正式接入后端后必须由后端接口返回对应字段，否则页面会降级或显示不完整：

- 普通用户只显示建议摄入热量区间。
- VIP 用户显示每种食物热量、总热量和 VIP 详细建议。
- 运动习惯卡片下方的具体运动项目建议。
- 普通用户和 VIP 用户功能隔离。
- 历史记录、体重记录表格的下拉筛选数据。
- AI 计划历史详情中的结构化饮食、运动和热量内容。

## 2. 已有文档建议同步的点

本次没有直接修改 `需求文档.md`、`接口文档.md`、`数据库文档.md`。

建议后续同步更新：

- `需求文档.md`
  - 普通用户热量建议应明确为“只展示建议摄入区间”。
  - VIP 用户热量建议应明确为“展示每种食物热量、总热量、目标区间和更详细说明”。
  - 运动计划应补充“具体运动项目建议”，例如热身、主项目、收尾拉伸。
- `接口文档.md`
  - `/plan/generate`、`/plan/history`、`/plan/{id}` 的 `planContent` 示例需要补充结构化字段。
  - `planLevel` 建议统一为 `BASIC` / `VIP`。如果后端返回中文“基础版”，前端目前可兼容展示，但接口文档最好统一枚举。
  - `/user/login`、`/user/current` 必须明确返回 `role: USER | VIP | ADMIN`。
- `数据库文档.md`
  - 当前 `plan.plan_content text` 可以继续保存 JSON 字符串，暂时不需要改表。
  - 如果后续要按单个食物热量、运动项目做统计或查询，再考虑拆分结构化表。

## 3. 删除模拟数据后仍会保留的前端能力

- 页面基础布局和视觉样式。
- 按钮统一样式、白色文字、文字居中。
- 健康档案保存后第三步完成打勾，并显示“查看健康画像”按钮。
- 健康画像查看 UI。
- 历史记录和体重记录的下拉筛选 UI。
- 普通用户不显示侧边栏“智能问答”入口。
- 普通用户和 VIP 用户在 AI 计划页面的不同展示逻辑。

这些能力属于前端已有逻辑，但显示内容是否完整取决于后端返回的数据。

## 4. 后端必须补齐的接口能力

### 4.1 用户角色与权限

接口：

- `POST /user/login`
- `GET /user/current`
- `POST /chat/ask`

要求：

- 登录和当前用户接口必须返回准确 `role`。
- 普通用户返回 `USER`。
- VIP 用户返回 `VIP`。
- 管理员返回 `ADMIN`。
- `/chat/ask` 必须在后端校验 VIP 权限，普通用户调用时返回 403。

如果后端不返回准确角色，普通用户可能看到 VIP 功能，或 VIP 用户看不到应有功能。

### 4.2 AI 计划生成次数

接口：

- `GET /plan/generate-count`
- `POST /plan/generate`

要求：

- 普通用户每天最多 2 次。
- VIP 用户每天最多 5 次。
- 返回字段建议包含 `used`、`limit`、`remaining`。
- 超过次数时后端应返回 403。

### 4.3 普通用户和 VIP 计划级别

接口：

- `POST /plan/generate`
- `GET /plan/history`
- `GET /plan/{id}`

要求：

- 普通用户生成的计划返回 `planLevel: "BASIC"`。
- VIP 用户生成的计划返回 `planLevel: "VIP"`。
- 前端会根据当前用户角色和 `planContent` 字段决定展示基础版还是 VIP 详情。

## 5. AI 计划内容字段要求

### 5.1 饮食热量字段

普通用户至少需要：

```json
{
  "calorie": {
    "target": "1650-1750 kcal",
    "advice": "普通用户先按建议区间控制总摄入。"
  }
}
```

VIP 用户需要：

```json
{
  "calorie": {
    "total": "约 1680 kcal",
    "target": "1650-1750 kcal",
    "foods": [
      { "name": "燕麦牛奶", "kcal": "360 kcal" },
      { "name": "水煮蛋", "kcal": "75 kcal" }
    ],
    "advice": "热量落在目标区间，适合继续保持。"
  }
}
```

如果缺少 `calorie.target`，普通用户页面的建议摄入区间会缺失。

如果缺少 `calorie.foods` 或 `calorie.total`，VIP 用户的单项食物热量和总热量会缺失。

### 5.2 运动项目建议字段

每一天的运动计划建议返回：

```json
{
  "day": "周一",
  "type": "快走 + 拉伸",
  "duration": "40 分钟",
  "intensity": "中低强度",
  "items": [
    { "name": "热身", "detail": "原地踏步 5 分钟，肩颈环绕各 10 次" },
    { "name": "主项目", "detail": "快走 25 分钟，保持能说话但略喘的速度" },
    { "name": "收尾", "detail": "小腿后侧、股四头肌、臀部各拉伸 30 秒" }
  ],
  "note": "适合减脂起始日，不追求速度，重点是稳定完成。"
}
```

如果缺少 `items`，运动习惯卡片下方的具体运动项目建议不会显示。

## 6. 建议响应示例

普通用户示例：

```json
{
  "id": 1,
  "planLevel": "BASIC",
  "planType": "COMBINED",
  "trendSummary": "体重趋势持续下降，建议继续保持。",
  "createTime": "2026-06-08T12:08:04",
  "planContent": {
    "dietPlan": [
      {
        "day": "周一",
        "breakfast": "燕麦牛奶 + 水煮蛋",
        "lunch": "鸡胸肉糙米饭 + 西兰花",
        "dinner": "番茄豆腐汤 + 小份红薯",
        "snack": "无糖酸奶",
        "calorie": {
          "target": "1650-1750 kcal",
          "advice": "普通用户先按建议区间控制总摄入。"
        }
      }
    ],
    "exercisePlan": [
      {
        "day": "周一",
        "type": "快走 + 拉伸",
        "duration": "40 分钟",
        "intensity": "中低强度",
        "items": [
          { "name": "热身", "detail": "原地踏步 5 分钟，肩颈环绕各 10 次" },
          { "name": "主项目", "detail": "快走 25 分钟，保持能说话但略喘的速度" },
          { "name": "收尾", "detail": "小腿后侧、股四头肌、臀部各拉伸 30 秒" }
        ],
        "note": "适合减脂起始日，不追求速度，重点是稳定完成。"
      }
    ]
  }
}
```

VIP 用户示例：

```json
{
  "id": 2,
  "planLevel": "VIP",
  "planType": "COMBINED",
  "trendSummary": "体重下降速度稳定，可维持当前热量区间。",
  "createTime": "2026-06-08T12:08:04",
  "planContent": {
    "dietPlan": [
      {
        "day": "周一",
        "breakfast": "燕麦牛奶 + 水煮蛋",
        "lunch": "鸡胸肉糙米饭 + 西兰花",
        "dinner": "番茄豆腐汤 + 小份红薯",
        "snack": "无糖酸奶",
        "calorie": {
          "total": "约 1680 kcal",
          "target": "1650-1750 kcal",
          "foods": [
            { "name": "燕麦牛奶", "kcal": "360 kcal" },
            { "name": "水煮蛋", "kcal": "75 kcal" },
            { "name": "鸡胸肉糙米饭", "kcal": "520 kcal" }
          ],
          "advice": "热量落在目标区间，适合继续保持。"
        }
      }
    ],
    "exercisePlan": [
      {
        "day": "周一",
        "type": "力量训练 + 有氧",
        "duration": "55 分钟",
        "intensity": "中等强度",
        "items": [
          { "name": "热身", "detail": "动态拉伸 8 分钟，髋、膝、踝充分活动" },
          { "name": "力量", "detail": "深蹲、俯卧撑、臀桥各 3 组，每组 10-12 次" },
          { "name": "有氧", "detail": "椭圆机 20 分钟，心率维持在最大心率 60%-70%" }
        ],
        "note": "力量训练优先保证动作质量，有氧阶段保持稳定呼吸。"
      }
    ],
    "vipDetail": {
      "calorieEstimate": "每日 1650-1750 kcal",
      "nutritionRatio": "蛋白质 30%，碳水 40%，脂肪 30%",
      "trainingLevel": "中等强度，建议每周 4 次",
      "weeklyReview": "每周固定同一时间称重 3 次，并观察围度变化。"
    }
  }
}
```

## 7. 历史记录和体重记录筛选字段

体重记录接口：

- `GET /weight/history`

每条记录必须包含：

- `recordDate`
- `weight`

历史计划接口：

- `GET /plan/history`

每条记录必须包含：

- `createTime`
- `planType`
- `planLevel`

前端下拉筛选选项来自接口返回的数据。如果字段为空，筛选下拉项会减少或无法筛选。

## 8. 后端验收清单

- 普通用户登录后 `role` 为 `USER`。
- VIP 用户登录后 `role` 为 `VIP`。
- 普通用户侧边栏不出现“智能问答”。
- 普通用户直接请求 `/chat/ask` 返回 403。
- 普通用户 AI 计划只显示建议摄入区间，不显示每种食物热量。
- VIP 用户 AI 计划显示总热量、每种食物热量和 VIP 详细建议。
- 运动计划每天都能显示具体项目建议。
- 历史计划列表包含 `createTime`、`planType`、`planLevel`。
- 体重记录列表包含 `recordDate`、`weight`。
- 删除 `src/api/mockData.js` 并关闭 mock 后，以上功能仍能通过真实接口展示。
