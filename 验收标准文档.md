# 智能健康助手项目验收标准文档

## 一、验收目标

本验收标准用于确认“智能健康助手——个性化饮食与运动规划系统”是否达到项目需求、接口约定和协作开发要求。

验收范围包括：

- 前端页面
- 后端接口
- 用户权限
- 数据库
- AI 计划生成
- Chroma RAG 检索
- VIP 智能问答
- 管理员后台
- 测试与文档

## 二、基础运行验收

### 1. 前端项目

验收项：

- Vue3 项目可以正常启动。
- 页面无明显控制台报错。
- 路由跳转正常。
- Axios 能正确调用后端接口。
- Token 可正常保存、携带和清除。

验收标准：

- 执行前端启动命令后，浏览器可访问系统页面。
- 登录前访问受保护页面会跳转登录页。
- 登录后刷新页面仍能保持登录状态。

### 2. 后端项目

验收项：

- Spring Boot 项目可以正常启动。
- MySQL 连接正常。
- Swagger/OpenAPI 页面可访问。
- JWT 认证生效。
- Flyway 迁移脚本可正常执行。

验收标准：

- 后端启动无报错。
- 数据库表自动创建成功。
- 未登录访问受保护接口返回 401。
- 无权限访问接口返回 403。

## 三、用户功能验收

### 1. 注册登录

验收项：

- 用户可以注册。
- 用户可以登录。
- 密码加密存储。
- 登录成功返回 JWT Token。
- 禁用用户不能登录。

验收标准：

- 用户名重复注册时返回提示。
- 密码不会以明文形式出现在数据库。
- 登录失败时页面有错误提示。
- 登录成功后进入首页。

### 2. 角色权限

验收项：

- 普通用户角色为 `USER`。
- VIP 用户角色为 `VIP`。
- 管理员角色为 `ADMIN`。
- 不同角色只能访问对应功能。

验收标准：

- 普通用户不能访问管理员接口。
- 普通用户不能使用智能问答。
- VIP 用户可以使用智能问答。
- 管理员可以进入后台管理页面。

## 四、健康档案验收

验收项：

- 用户可以新增健康档案。
- 用户可以修改健康档案。
- 用户可以查询自己的健康档案。
- 管理员可以查看用户健康档案。
- 用户不能访问其他用户健康档案。

验收标准：

- 健康档案字段保存正确。
- 修改后再次查询能看到最新数据。
- 越权访问返回 403。

## 五、体重记录验收

验收项：

- 用户可以添加体重记录。
- 用户可以查询体重历史。
- 用户可以删除自己的错误记录。
- 体重记录按日期展示。
- 前端可以展示体重趋势折线图。

验收标准：

- 添加体重后列表中能看到新记录。
- 删除后列表中不再显示该记录。
- 用户不能删除其他用户体重记录。
- 折线图数据与接口返回数据一致。

## 六、AI 计划生成验收

### 1. 生成次数限制

验收项：

- 普通用户每天最多生成 2 次 AI 计划。
- VIP 用户每天最多生成 5 次 AI 计划。
- 超过次数后不能继续生成。

验收标准：

- 普通用户第 3 次生成时返回次数已用完提示。
- VIP 用户第 6 次生成时返回次数已用完提示。
- `/plan/generate-count` 返回已用次数、限制次数和剩余次数。

### 2. 计划内容

验收项：

- 普通用户生成基础饮食和运动计划。
- VIP 用户生成详细饮食和运动计划。
- 生成结果保存到数据库。
- 历史计划可以查询。

验收标准：

- 普通用户计划包含早餐、午餐、晚餐、加餐和运动安排。
- VIP 计划额外包含热量估算、营养比例、训练强度分级、每周复盘建议。
- 生成后可在历史计划中看到记录。
- 计划详情可以正常打开。

### 3. 体重趋势优化

验收项：

- 后端读取用户历史体重记录。
- 系统分析体重变化趋势。
- 趋势结果加入 AI Prompt。

验收标准：

- 有体重记录时，计划结果中包含趋势分析。
- 无体重记录时，系统能正常生成计划并给出基础建议。

## 七、Chroma RAG 验收

### 1. 知识库存储

验收项：

- 健康知识直接保存到 Chroma。
- Chroma 中包含 document 和 metadata。
- metadata 包含 `category`、`title`、`keywords`、`status`、`level`。

验收标准：

- 管理员新增健康知识后，可在知识库列表查询到。
- 修改健康知识后，检索结果使用新内容。
- 禁用知识不参与检索。
- 删除知识后不能再被查询或检索。

### 2. AI 计划 RAG 检索

验收项：

- 生成 AI 计划前检索 Chroma。
- 每次检索 Top 3 到 Top 5 条知识。
- 普通用户检索 `basic` 和 `all`。
- VIP 用户检索 `basic`、`vip` 和 `all`。

验收标准：

- AI 计划详情中可展示参考知识标题。
- 普通用户计划不会引用 `vip` 专属知识。
- VIP 用户计划可以引用 `vip` 专属知识。

## 八、VIP 智能问答验收

验收项：

- 普通用户不能使用自由问答。
- VIP 用户可以使用自由问答。
- 问答前先检索 Chroma。
- 问答检索 `vip` 和 `all` 级别知识。
- 回答返回参考知识标题。
- 回答包含非医疗建议提示。

验收标准：

- 普通用户访问问答页显示 VIP 专属提示。
- 普通用户调用 `/chat/ask` 返回 403。
- VIP 用户输入问题后可以得到回答。
- 回答末尾包含“本回答仅供健康管理参考，不能替代专业医疗建议。”
- Chroma 或 DeepSeek 调用失败时页面有错误提示。

## 九、管理员后台验收

验收项：

- 管理员可以登录后台。
- 管理员可以查看用户列表。
- 管理员可以查看用户详情。
- 管理员可以启用、禁用用户。
- 管理员可以查看用户体重记录。
- 管理员可以查看用户 AI 计划记录。
- 管理员可以管理健康知识库。

验收标准：

- 非管理员访问后台接口返回 403。
- 禁用用户后，该用户不能登录。
- 管理员新增、修改、删除、启用、禁用健康知识均生效。

## 十、前端页面验收

### 1. 用户端页面

验收项：

- 登录页
- 注册页
- 首页
- 健康档案页
- AI 计划生成页
- 体重记录页
- 历史计划页
- 智能问答页

验收标准：

- 页面跳转正常。
- 表单校验正常。
- 加载状态正常。
- 空数据状态正常。
- 接口失败提示清楚。

### 2. GSAP 登录动画

验收项：

- 登录页有健康数据流动画。
- 表单进入动画正常。
- 健康数据卡片动画正常。
- 登录成功后有过渡动画。

验收标准：

- 动画不影响表单输入。
- 动画不影响登录接口请求。
- 登录成功后能正常进入首页。

## 十一、接口验收

验收项：

- 接口路径统一使用 `/api/v1` 前缀。
- 请求和响应使用 JSON。
- 需要登录的接口必须校验 JWT。
- 接口返回统一响应格式。
- Swagger/OpenAPI 文档与实际接口一致。

验收标准：

- Postman 测试集合全部通过。
- 关键接口正常、异常、无权限场景均有测试。
- 接口字段与前端使用字段一致。

## 十二、数据库验收

验收项：

- MySQL 表结构与数据库文档一致。
- Flyway 脚本可自动创建表。
- 用户名唯一约束生效。
- 用户和业务数据关联正确。
- 计划生成次数记录正确。

验收标准：

- 新环境执行迁移脚本后可直接运行项目。
- 数据库中不存在明文密码。
- 普通用户只能查询自己的数据。
- `plan_generate_record` 能正确记录每日生成次数。

## 十三、安全验收

验收项：

- 密码使用 BCrypt 加密。
- DeepSeek API Key 不出现在前端代码。
- JWT Token 不打印到日志。
- 普通用户不能访问其他用户数据。
- 普通用户不能访问 VIP 问答。
- 非管理员不能访问后台接口。

验收标准：

- 通过接口测试验证 401、403 场景。
- 检查前端代码中无 API Key。
- 检查日志中无密码、Token、API Key。

## 十四、测试验收

验收项：

- 后端核心 Service 有单元测试。
- Controller 接口有集成测试。
- Postman 集合覆盖关键接口。
- RAG 问答有测试样例。

验收标准：

- 单元测试通过。
- 接口测试通过。
- 关键业务流程测试通过。
- RAG 回答基本符合知识库内容。

## 十五、文档验收

验收项：

- 需求文档完整。
- 接口文档完整。
- 数据库文档完整。
- 开发分工与 Git 工作流文档完整。
- 验收标准文档完整。

验收标准：

- 文档内容与实际实现一致。
- 接口变更后同步更新接口文档。
- 数据库变更后同步更新数据库文档。

## 十六、最终验收清单

| 模块 | 是否通过 |
| --- | --- |
| 前端项目启动 | 待验收 |
| 后端项目启动 | 待验收 |
| 注册登录 | 待验收 |
| JWT 认证 | 待验收 |
| 角色权限 | 待验收 |
| 健康档案 | 待验收 |
| 体重记录 | 待验收 |
| AI 计划生成 | 待验收 |
| 生成次数限制 | 待验收 |
| Chroma RAG 检索 | 待验收 |
| VIP 智能问答 | 待验收 |
| 管理员后台 | 待验收 |
| GSAP 登录动画 | 待验收 |
| 数据库迁移 | 待验收 |
| 接口文档一致性 | 待验收 |
| 安全检查 | 待验收 |
| 测试通过 | 待验收 |
