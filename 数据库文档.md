# 智能健康助手数据库文档

## 一、数据库概述

### 1. 数据库类型

```text
MySQL 8.0
```

### 2. 数据库名称

```text
smart_health
```

### 3. 字符集与排序规则

```text
utf8mb4
utf8mb4_general_ci
```

### 4. 表设计范围

MySQL 负责保存系统业务数据：

- 用户账号
- 健康档案
- 体重记录
- AI 计划记录
- AI 计划生成次数

Chroma 负责保存 RAG 健康知识库：

- 健康知识正文 document
- 知识 metadata
- 向量数据

## 二、命名规范

### 1. 表命名

- 使用小写英文。
- 多个单词使用下划线连接。
- 表名使用单数或业务名，不使用拼音。

示例：

```text
user
health_profile
weight_record
plan
plan_generate_record
```

### 2. 字段命名

- 使用小写英文。
- 多个单词使用下划线连接。
- 数据库字段使用 snake_case。
- 前端接口字段使用 camelCase，由后端 DTO 转换。

示例：

```text
user_id
create_time
vip_expire_time
```

### 3. 时间字段

常用时间字段：

```text
create_time
update_time
```

时间类型统一使用：

```text
datetime
```

## 三、表关系说明

```text
user 1 ----- 1 health_profile
user 1 ----- N weight_record
user 1 ----- N plan
user 1 ----- N plan_generate_record
user 1 ----- 1 user_ai_config
```

说明：

- 一个用户最多对应一份健康档案。
- 一个用户可以有多条体重记录。
- 一个用户可以有多条 AI 计划记录。
- 一个用户每天对应一条 AI 计划生成次数记录。
- 一个用户最多对应一份 AI 配置。

## 四、用户表 user

### 1. 表说明

保存用户账号、密码、角色、状态和 VIP 到期时间。

### 2. 表结构

| 字段名 | 类型 | 是否为空 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | 主键 |
| username | varchar(50) | 否 | 无 | 用户名 |
| password | varchar(255) | 否 | 无 | BCrypt 加密后的密码 |
| role | varchar(20) | 否 | USER | 角色：USER、VIP、ADMIN |
| status | int | 否 | 1 | 状态：1 正常，0 禁用 |
| vip_expire_time | datetime | 是 | null | VIP 到期时间 |
| create_time | datetime | 否 | 当前时间 | 创建时间 |
| update_time | datetime | 否 | 当前时间 | 更新时间 |

### 3. 约束与索引

| 名称 | 类型 | 字段 | 说明 |
| --- | --- | --- | --- |
| pk_user | 主键 | id | 用户主键 |
| uk_user_username | 唯一索引 | username | 用户名唯一 |
| idx_user_role | 普通索引 | role | 按角色查询 |
| idx_user_status | 普通索引 | status | 按账号状态查询 |

### 4. 建表 SQL

```sql
CREATE TABLE `user` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `username` varchar(50) NOT NULL,
  `password` varchar(255) NOT NULL,
  `role` varchar(20) NOT NULL DEFAULT 'USER',
  `status` int NOT NULL DEFAULT 1,
  `vip_expire_time` datetime DEFAULT NULL,
  `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_username` (`username`),
  KEY `idx_user_role` (`role`),
  KEY `idx_user_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;
```

## 五、健康档案表 health_profile

### 1. 表说明

保存用户的健康档案信息，用于 AI 计划生成。

### 2. 表结构

| 字段名 | 类型 | 是否为空 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | 主键 |
| user_id | bigint | 否 | 无 | 用户 ID |
| age | int | 否 | 无 | 年龄 |
| gender | varchar(20) | 否 | 无 | 性别 |
| height | decimal(5,2) | 否 | 无 | 身高，单位 cm |
| weight | decimal(5,2) | 否 | 无 | 当前体重，单位 kg |
| activity_level | varchar(50) | 否 | 无 | 活动水平 |
| diet_preference | varchar(255) | 是 | null | 饮食偏好 |
| goal | varchar(100) | 否 | 无 | 健康目标 |
| create_time | datetime | 否 | 当前时间 | 创建时间 |
| update_time | datetime | 否 | 当前时间 | 更新时间 |

### 3. 约束与索引

| 名称 | 类型 | 字段 | 说明 |
| --- | --- | --- | --- |
| pk_health_profile | 主键 | id | 档案主键 |
| uk_health_profile_user_id | 唯一索引 | user_id | 一个用户一份健康档案 |
| idx_health_profile_goal | 普通索引 | goal | 按健康目标查询 |

### 4. 建表 SQL

```sql
CREATE TABLE `health_profile` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL,
  `age` int NOT NULL,
  `gender` varchar(20) NOT NULL,
  `height` decimal(5,2) NOT NULL,
  `weight` decimal(5,2) NOT NULL,
  `activity_level` varchar(50) NOT NULL,
  `diet_preference` varchar(255) DEFAULT NULL,
  `goal` varchar(100) NOT NULL,
  `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_health_profile_user_id` (`user_id`),
  KEY `idx_health_profile_goal` (`goal`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;
```

## 六、体重记录表 weight_record

### 1. 表说明

保存用户体重记录，用于趋势展示和 AI 记忆优化。

### 2. 表结构

| 字段名 | 类型 | 是否为空 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | 主键 |
| user_id | bigint | 否 | 无 | 用户 ID |
| weight | decimal(5,2) | 否 | 无 | 体重，单位 kg |
| record_date | date | 否 | 无 | 记录日期 |
| create_time | datetime | 否 | 当前时间 | 创建时间 |

### 3. 约束与索引

| 名称 | 类型 | 字段 | 说明 |
| --- | --- | --- | --- |
| pk_weight_record | 主键 | id | 体重记录主键 |
| idx_weight_record_user_date | 普通索引 | user_id, record_date | 查询用户体重历史 |

### 4. 建表 SQL

```sql
CREATE TABLE `weight_record` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL,
  `weight` decimal(5,2) NOT NULL,
  `record_date` date NOT NULL,
  `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_weight_record_user_date` (`user_id`, `record_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;
```

## 七、AI 计划表 plan

### 1. 表说明

保存 AI 生成的饮食计划、运动计划和趋势分析结果。

### 2. 表结构

| 字段名 | 类型 | 是否为空 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | 主键 |
| user_id | bigint | 否 | 无 | 用户 ID |
| plan_type | varchar(30) | 否 | COMBINED | 计划类型：DIET、EXERCISE、COMBINED |
| plan_level | varchar(20) | 否 | BASIC | 计划级别：BASIC、VIP |
| plan_content | text | 否 | 无 | AI 计划内容，JSON 字符串 |
| trend_summary | varchar(500) | 是 | null | 体重趋势分析 |
| create_time | datetime | 否 | 当前时间 | 创建时间 |

### 3. 约束与索引

| 名称 | 类型 | 字段 | 说明 |
| --- | --- | --- | --- |
| pk_plan | 主键 | id | 计划主键 |
| idx_plan_user_create_time | 普通索引 | user_id, create_time | 查询用户历史计划 |
| idx_plan_type | 普通索引 | plan_type | 按计划类型查询 |
| idx_plan_level | 普通索引 | plan_level | 按计划级别查询 |

### 4. 建表 SQL

```sql
CREATE TABLE `plan` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL,
  `plan_type` varchar(30) NOT NULL DEFAULT 'COMBINED',
  `plan_level` varchar(20) NOT NULL DEFAULT 'BASIC',
  `plan_content` text NOT NULL,
  `trend_summary` varchar(500) DEFAULT NULL,
  `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_plan_user_create_time` (`user_id`, `create_time`),
  KEY `idx_plan_type` (`plan_type`),
  KEY `idx_plan_level` (`plan_level`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;
```

## 八、AI 计划生成记录表 plan_generate_record

### 1. 表说明

记录用户每日 AI 计划生成次数，用于限制普通用户和 VIP 用户的生成次数。

### 2. 表结构

| 字段名 | 类型 | 是否为空 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | 主键 |
| user_id | bigint | 否 | 无 | 用户 ID |
| generate_date | date | 否 | 无 | 生成日期 |
| generate_count | int | 否 | 0 | 当日生成次数 |
| create_time | datetime | 否 | 当前时间 | 创建时间 |
| update_time | datetime | 否 | 当前时间 | 更新时间 |

### 3. 约束与索引

| 名称 | 类型 | 字段 | 说明 |
| --- | --- | --- | --- |
| pk_plan_generate_record | 主键 | id | 主键 |
| uk_plan_generate_user_date | 唯一索引 | user_id, generate_date | 一个用户每天一条生成记录 |

### 4. 建表 SQL

```sql
CREATE TABLE `plan_generate_record` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL,
  `generate_date` date NOT NULL,
  `generate_count` int NOT NULL DEFAULT 0,
  `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_plan_generate_user_date` (`user_id`, `generate_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;
```

## 九、VIP 激活码表 vip_activation_code

### 1. 表说明

存储 VIP 激活码，管理员生成激活码，普通用户使用激活码升级为 VIP 身份。

### 2. 表结构

| 字段名 | 类型 | 是否为空 | 默认值 | 说明 |
|--------|------|----------|--------|------|
| id | bigint | 否 | 自增 | 主键 |
| code | varchar(32) | 否 | 无 | 激活码，格式 VIP-XXXXXXXX |
| status | tinyint | 否 | 0 | 0=未使用，1=已使用，2=已过期 |
| created_by | bigint | 否 | 无 | 创建人（管理员 ID）|
| used_by | bigint | 是 | NULL | 使用人（用户 ID）|
| created_at | datetime | 否 | 当前时间 | 创建时间 |
| expires_at | datetime | 否 | 无 | 过期时间 |
| used_at | datetime | 是 | NULL | 使用时间 |

### 3. 约束与索引

| 名称 | 类型 | 字段 | 说明 |
|------|------|------|------|
| pk_vip_code | 主键 | id | 主键 |
| uk_code | 唯一索引 | code | 激活码唯一 |

### 4. 建表 SQL

```sql
CREATE TABLE `vip_activation_code` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `code` varchar(32) NOT NULL,
  `status` tinyint NOT NULL DEFAULT '0',
  `created_by` bigint NOT NULL,
  `used_by` bigint DEFAULT NULL,
  `created_at` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `expires_at` datetime NOT NULL,
  `used_at` datetime DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_code` (`code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;
```

### 5. 业务规则

- 激活码格式：`VIP-XXXXXXXX`（8 位大写十六进制）
- 有效期：创建后 7 天过期
- 状态流转：未使用（0）→ 已使用（1）或已过期（2）
- 激活后用户角色变为 `VIP`，`vip_expire_time` 设置为当前时间 + 30 天

## 十、用户 AI 配置表 user_ai_config

### 1. 表说明

保存用户自定义的 AI 配置，支持用户选择 AI 服务提供商和自定义 API Key。

### 2. 表结构

| 字段名 | 类型 | 是否为空 | 默认值 | 说明 |
|--------|------|----------|--------|------|
| id | bigint | 否 | 自增 | 主键 |
| user_id | bigint | 否 | 无 | 用户 ID，唯一 |
| provider | varchar(20) | 否 | DEFAULT | 提供者：DEFAULT / CUSTOM / LOCAL |
| api_key | varchar(500) | 是 | null | 用户自定义 API Key |
| api_url | varchar(500) | 是 | null | 自定义 API 地址 |
| model | varchar(100) | 是 | null | 模型名称 |
| created_at | datetime | 否 | 无 | 创建时间 |
| updated_at | datetime | 否 | 无 | 更新时间 |

### 3. 约束与索引

| 名称 | 类型 | 字段 | 说明 |
|------|------|------|------|
| pk_user_ai_config | 主键 | id | 主键 |
| uk_user_ai_config_user_id | 唯一索引 | user_id | 每个用户一条配置 |

### 4. 建表 SQL

```sql
CREATE TABLE user_ai_config (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL UNIQUE,
    provider VARCHAR(20) NOT NULL DEFAULT 'DEFAULT' COMMENT 'DEFAULT / CUSTOM / LOCAL',
    api_key VARCHAR(500) COMMENT '用户自定义 API Key',
    api_url VARCHAR(500) COMMENT '自定义 API 地址',
    model VARCHAR(100) COMMENT '模型名称',
    created_at DATETIME NOT NULL,
    updated_at DATETIME NOT NULL,
    FOREIGN KEY (user_id) REFERENCES `user`(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户 AI 配置';
```

### 5. 业务规则

- 一个用户最多一条配置（UNIQUE(user_id)）
- `provider=DEFAULT`：使用服务器 DeepSeek Key，忽略 api_key/api_url/model
- `provider=CUSTOM`：使用用户提交的自定义 API Key 调用 DeepSeek
- `provider=LOCAL`：使用本地 Ollama 模型，需要 VIP 身份
- API Key 在数据库中明文存储，响应中脱敏展示
- 保存时自动测试 Key 连通性

## 十一、Chroma 健康知识库说明

### 1. 存储方式

健康知识库不在 MySQL 中建表，直接存储在 Chroma 中。

每条知识包含：

```text
id
document
metadata
embedding
```

### 2. document

`document` 保存健康知识正文。

示例：

```text
减脂期间应控制总热量摄入，增加优质蛋白，减少高糖、高油、高热量食物。
```

### 3. metadata

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| category | string | 知识分类 |
| title | string | 知识标题 |
| keywords | string | 关键词 |
| status | string | enabled 或 disabled |
| level | string | basic、vip 或 all |

### 4. Chroma 数据示例

```json
{
  "id": "knowledge_fat_loss_001",
  "document": "减脂期间应控制总热量摄入，增加优质蛋白，减少高糖、高油、高热量食物。",
  "metadata": {
    "category": "减脂饮食",
    "title": "减脂期间饮食原则",
    "keywords": "减脂,热量,蛋白质,低糖",
    "status": "enabled",
    "level": "all"
  }
}
```

### 5. 检索规则

AI 计划生成：

- 普通用户检索 `basic` 和 `all`。
- VIP 用户检索 `basic`、`vip` 和 `all`。
- 仅检索 `status = enabled` 的知识。
- 每次取 Top 3 到 Top 5 条结果。

VIP 智能问答：

- 仅 VIP 用户可使用。
- 检索 `vip` 和 `all`。
- 仅检索 `status = enabled` 的知识。
- 每次取 Top 3 到 Top 5 条结果。

## 十二、枚举说明

### 1. 用户角色 role

| 值 | 说明 |
| --- | --- |
| USER | 普通用户 |
| VIP | VIP 用户 |
| ADMIN | 管理员 |

### 2. 用户状态 status

| 值 | 说明 |
| --- | --- |
| 1 | 正常 |
| 0 | 禁用 |

### 3. 计划类型 plan_type

| 值 | 说明 |
| --- | --- |
| DIET | 饮食计划 |
| EXERCISE | 运动计划 |
| COMBINED | 综合计划 |

### 4. 计划级别 plan_level

| 值 | 说明 |
| --- | --- |
| BASIC | 基础计划 |
| VIP | VIP 详细计划 |

### 5. 知识状态 status

| 值 | 说明 |
| --- | --- |
| enabled | 启用 |
| disabled | 禁用 |

### 6. 知识级别 level

| 值 | 说明 |
| --- | --- |
| basic | 基础知识 |
| vip | VIP 专属知识 |
| all | 所有用户可用 |

### 7. AI Provider

| 值 | 说明 |
|----|------|
| DEFAULT | 使用服务器默认 DeepSeek Key |
| CUSTOM | 使用用户自定义 API Key |
| LOCAL | 使用本地 Ollama 模型 |

## 十三、Flyway 迁移规范

### 1. 脚本目录

```text
src/main/resources/db/migration
```

### 2. 命名规则

```text
V{版本号}__{描述}.sql
```

示例：

```text
V1__create_user_table.sql
V2__create_health_profile_table.sql
V3__create_weight_record_table.sql
V4__create_plan_table.sql
V5__create_plan_generate_record_table.sql
V6__seed_admin_user.sql
V7__create_vip_activation_code_table.sql
V8__create_user_ai_config_table.sql
```

### 3. 使用要求

- 已执行的迁移脚本不能修改。
- 新增表或字段必须新增迁移脚本。
- 删除字段、修改字段类型等破坏性操作需要团队确认。
- Flyway 只管理 MySQL 结构，不管理 Chroma 向量数据。

## 十四、初始化数据建议

### 1. 管理员账号

建议初始化一个管理员账号。当前 seed 脚本 `V6__seed_admin_user.sql` 默认密码为 `Admin@2026`（BCrypt 加密）。

```sql
INSERT INTO `user` (`username`, `password`, `role`, `status`)
VALUES ('admin', '$2a$10$...bcrypt_hash...', 'ADMIN', 1);
```

说明：

- 密码必须使用 BCrypt 加密后写入。
- 不允许使用明文密码。

### 2. Chroma 健康知识

建议初始化以下分类知识：

- 减脂饮食
- 增肌饮食
- 控糖饮食
- 低盐饮食
- 有氧运动
- 力量训练
- BMI 分类
- 体重管理
- 运动注意事项

## 十五、数据安全要求

- 用户密码必须使用 BCrypt 加密。
- 数据库中仅保存用户主动配置的自定义 API Key（`user_ai_config.api_key`），明文存储，不保存服务器 Key。
- 数据库中不保存 JWT Token。
- 普通用户只能访问自己的业务数据。
- 管理员接口必须单独做权限校验。
- 删除数据前必须校验数据归属或管理员权限。
