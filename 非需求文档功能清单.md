# 前端非需求文档功能清单

本清单基于当前 `smart-health-frontend` 前端代码与 `需求文档.md` 的对照结果整理。

开发调试、体验登录、环境变量、本地预览状态等临时功能不纳入本清单。

## 核查范围

已核查：

- 前端页面与路由
- 前端 API 模块
- 根目录接口文档
- 根目录数据库文档

当前没有发现需求文档之外的真实后端业务接口，也没有发现需求文档之外的数据库表或字段。

## 1. 升级 VIP 页面

相关文件：

- `smart-health-frontend/src/views/VipUpgradeView.vue`
- `smart-health-frontend/src/router/index.js`
- `smart-health-frontend/src/layouts/MainLayout.vue`
- `smart-health-frontend/src/views/HomeView.vue`

当前前端存在独立的 `/vip` 页面，页面名称为“升级 VIP”，用于展示普通用户和 VIP 用户能力差异，并提供“立即升级 VIP”按钮。

需求文档已补充“VIP 升级说明页面”，明确该页面仅作为前端能力说明和升级入口展示，不接入支付、订单、续期或真实开通流程。

结论：该页面已纳入需求文档，不再属于需求文档外功能。

## 2. `/vip` 前端路由与入口

相关文件：

- `smart-health-frontend/src/router/index.js`
- `smart-health-frontend/src/layouts/MainLayout.vue`
- `smart-health-frontend/src/views/HomeView.vue`

当前前端新增了 `/vip` 路由，并在侧边栏、首页主按钮、首页快捷入口中提供跳转。

需求文档列出的用户端页面包括：

- 登录页
- 注册页
- 首页
- 健康档案页
- AI 计划生成页
- 体重记录页
- 历史计划页
- 智能问答页

需求文档已列出“升级 VIP 页”，并说明其用于展示普通用户和 VIP 用户能力差异。

结论：`/vip` 前端路由与入口已纳入需求文档。

## 3. 侧边栏健康脉冲动画

相关文件：

- `smart-health-frontend/src/layouts/MainLayout.vue`

需求文档明确要求登录页 GSAP 健康数据流动画，以及首页进入动画。

当前侧边栏额外实现了 SVG 心率线循环动画。该动画风格与项目主题一致，但需求文档没有单独要求侧边栏动画。

结论：该功能属于额外视觉增强，不是需求文档明确功能。

## 4. 首页开发式待办文案

相关文件：

- `smart-health-frontend/src/views/HomeView.vue`

当前首页“今日动作”已替换为面向用户的健康任务文案：

- 完善健康档案
- 记录今日体重
- 查看 AI 健康计划

结论：首页不再展示开发任务式文案。

## 5. 管理员后台聚合占位页

相关文件：

- `smart-health-frontend/src/router/index.js`
- `smart-health-frontend/src/layouts/AdminLayout.vue`
- `smart-health-frontend/src/views/admin/AdminLoginView.vue`
- `smart-health-frontend/src/views/admin/AdminDashboardView.vue`

当前已改为独立管理员登录页和后台管理页面，不再保留 `/admin` 聚合占位页。

需求文档中管理员端页面包括：

- 管理员登录页
- 用户管理页
- 用户详情页
- 体重记录管理页
- AI 计划记录管理页
- 健康知识库管理页

结论：管理员聚合占位页不再作为前端交付页面。

## 6. 后端接口新增情况

相关文件：

- `smart-health-frontend/src/api/modules/auth.js`
- `smart-health-frontend/src/api/modules/profile.js`
- `smart-health-frontend/src/api/modules/weight.js`
- `smart-health-frontend/src/api/modules/plan.js`
- `smart-health-frontend/src/api/modules/chat.js`
- `smart-health-frontend/src/api/modules/admin.js`
- `smart-health-frontend/src/api/request.js`

当前前端 API 模块中封装的接口包括：

- `/user/login`
- `/user/register`
- `/user/current`
- `/profile`
- `/weight/add`
- `/weight/history`
- `/weight/{id}`
- `/plan/generate`
- `/plan/history`
- `/plan/{id}`
- `/plan/generate-count`
- `/chat/ask`
- `/admin/users`
- `/admin/users/{id}/enable`
- `/admin/users/{id}/disable`

以上接口均能在 `需求文档.md` 的接口设计中找到对应项。

当前没有发现类似 `/vip/upgrade`、`/user/upgrade-vip`、`/admin/dashboard` 这类需求文档外的新增后端接口。

结论：暂未发现需求文档外的真实后端接口新增。

## 7. 数据库新增情况

相关文件：

- `数据库文档.md`
- `需求文档.md`

当前数据库文档中的核心表包括：

- `user`
- `health_profile`
- `weight_record`
- `plan`
- `plan_generate_record`

这些表都能在 `需求文档.md` 的数据库设计中找到对应项。

当前没有发现需求文档外的新增表，例如：

- VIP 订单表
- VIP 支付表
- 聊天记录表
- 管理员操作日志表

当前也没有发现需求文档外的新增字段。

结论：暂未发现需求文档外的数据库表或字段新增。
