# 智能健康助手测试文档

## 一、测试目标

验证智能健康助手系统是否满足需求文档、接口文档、数据库文档和验收标准文档中的要求。

测试重点：

- 用户注册登录
- JWT 认证与角色权限
- 健康档案管理
- 体重记录管理
- AI 计划生成
- 普通用户和 VIP 用户次数限制
- Chroma RAG 检索
- VIP 智能健康问答
- 管理员后台
- 前后端联调
- 数据安全

## 二、测试范围

### 1. 前端测试范围

- 登录页
- 注册页
- 首页
- 健康档案页
- AI 计划生成页
- 体重记录页
- 历史计划页
- 智能问答页
- 管理员后台页面
- GSAP 登录动画

### 2. 后端测试范围

- 用户接口
- 健康档案接口
- 体重记录接口
- AI 计划接口
- VIP 智能问答接口
- 管理员接口
- 知识库接口
- JWT 权限校验
- MySQL 数据读写
- Chroma 检索
- DeepSeek API 调用

### 3. 不测试范围

- DeepSeek 模型本身的生成能力。
- Chroma 底层向量算法准确性。
- 浏览器极低版本兼容性。

## 三、测试环境

### 1. 前端环境

| 项目 | 说明 |
| --- | --- |
| 框架 | Vue3 |
| UI 组件 | Element Plus |
| 请求库 | Axios |
| 动画库 | GSAP |
| 浏览器 | Chrome 最新版 |

### 2. 后端环境

| 项目 | 说明 |
| --- | --- |
| 框架 | Spring Boot 3.2 |
| ORM | MyBatis |
| 权限 | JWT + Spring Security |
| 数据库 | MySQL 8.0 |
| RAG | Chroma |
| AI 服务 | DeepSeek API |

### 3. 测试工具

| 工具 | 用途 |
| --- | --- |
| Postman | 接口测试 |
| Swagger UI | 接口文档验证 |
| Navicat | 数据库检查 |
| 浏览器控制台 | 前端错误检查 |
| JUnit 5 | 后端单元测试 |
| MockMvc | 后端接口测试 |

## 四、测试账号

| 角色 | 用户名 | 密码 | 说明 |
| --- | --- | --- | --- |
| 普通用户 | user_test | 123456 | USER 角色 |
| VIP 用户 | vip_test | 123456 | VIP 角色 |
| 管理员 | admin | 123456 | ADMIN 角色 |

说明：

- 测试密码仅用于本地测试。
- 数据库中密码必须为 BCrypt 加密结果。
- 不允许在正式环境使用上述密码。

## 五、接口测试用例

### 1. 用户注册

| 用例编号 | 测试场景 | 请求数据 | 预期结果 |
| --- | --- | --- | --- |
| U-001 | 正常注册 | 新用户名和密码 | 注册成功 |
| U-002 | 用户名为空 | username 为空 | 返回参数错误 |
| U-003 | 密码为空 | password 为空 | 返回参数错误 |
| U-004 | 用户名重复 | 已存在 username | 返回用户名已存在 |

### 2. 用户登录

| 用例编号 | 测试场景 | 请求数据 | 预期结果 |
| --- | --- | --- | --- |
| U-005 | 正常登录 | 正确用户名和密码 | 返回 Token |
| U-006 | 密码错误 | 错误密码 | 返回用户名或密码错误 |
| U-007 | 用户不存在 | 不存在用户名 | 返回用户名或密码错误 |
| U-008 | 禁用用户登录 | status = 0 | 返回账号已被禁用 |

### 3. 当前用户信息

| 用例编号 | 测试场景 | 请求头 | 预期结果 |
| --- | --- | --- | --- |
| U-009 | Token 正确 | Bearer Token | 返回当前用户信息 |
| U-010 | 不带 Token | 无 | 返回 401 |
| U-011 | Token 无效 | 错误 Token | 返回 401 |

## 六、健康档案测试用例

| 用例编号 | 测试场景 | 操作 | 预期结果 |
| --- | --- | --- | --- |
| P-001 | 新增健康档案 | 提交完整档案 | 保存成功 |
| P-002 | 查询健康档案 | 登录后查询 | 返回当前用户档案 |
| P-003 | 修改健康档案 | 修改体重或目标 | 修改成功 |
| P-004 | 参数缺失 | 缺少年龄或身高 | 返回参数错误 |
| P-005 | 越权访问 | 访问其他用户档案 | 返回 403 |

## 七、体重记录测试用例

| 用例编号 | 测试场景 | 操作 | 预期结果 |
| --- | --- | --- | --- |
| W-001 | 添加体重记录 | 提交日期和体重 | 添加成功 |
| W-002 | 查询体重历史 | 查询当前用户记录 | 返回体重列表 |
| W-003 | 删除体重记录 | 删除自己的记录 | 删除成功 |
| W-004 | 删除他人记录 | 删除其他用户记录 | 返回 403 |
| W-005 | 体重为空 | weight 为空 | 返回参数错误 |
| W-006 | 日期为空 | recordDate 为空 | 返回参数错误 |

## 八、AI 计划测试用例

### 1. 普通用户计划生成

| 用例编号 | 测试场景 | 操作 | 预期结果 |
| --- | --- | --- | --- |
| A-001 | 第一次生成 | USER 调用 /plan/generate | 生成成功 |
| A-002 | 第二次生成 | USER 再次调用 | 生成成功 |
| A-003 | 第三次生成 | USER 第三次调用 | 返回次数已用完 |
| A-004 | 查询次数 | 调用 /plan/generate-count | limitCount 为 2 |

### 2. VIP 用户计划生成

| 用例编号 | 测试场景 | 操作 | 预期结果 |
| --- | --- | --- | --- |
| A-005 | VIP 生成计划 | VIP 调用 /plan/generate | 生成成功 |
| A-006 | VIP 第五次生成 | 第五次调用 | 生成成功 |
| A-007 | VIP 第六次生成 | 第六次调用 | 返回次数已用完 |
| A-008 | 查询次数 | 调用 /plan/generate-count | limitCount 为 5 |

### 3. 计划内容

| 用例编号 | 测试场景 | 检查内容 | 预期结果 |
| --- | --- | --- | --- |
| A-009 | 普通用户计划 | planLevel | BASIC |
| A-010 | VIP 用户计划 | planLevel | VIP |
| A-011 | VIP 详细内容 | 热量、营养比例、训练强度、复盘建议 | 内容存在 |
| A-012 | 历史计划 | 查询 /plan/history | 返回历史记录 |
| A-013 | 计划详情 | 查询 /plan/{id} | 返回计划详情 |

## 九、RAG 测试用例

### 1. 知识库管理

| 用例编号 | 测试场景 | 操作 | 预期结果 |
| --- | --- | --- | --- |
| R-001 | 新增知识 | 管理员新增健康知识 | Chroma 写入成功 |
| R-002 | 查询知识 | 查询知识列表 | 返回新增知识 |
| R-003 | 修改知识 | 修改 document 或 metadata | 修改成功 |
| R-004 | 禁用知识 | 调用禁用接口 | status 变为 disabled |
| R-005 | 启用知识 | 调用启用接口 | status 变为 enabled |
| R-006 | 删除知识 | 调用删除接口 | 知识不再返回 |

### 2. 计划生成 RAG

| 用例编号 | 测试场景 | 操作 | 预期结果 |
| --- | --- | --- | --- |
| R-007 | 普通用户检索 | USER 生成计划 | 只检索 basic/all |
| R-008 | VIP 用户检索 | VIP 生成计划 | 可检索 basic/vip/all |
| R-009 | 禁用知识过滤 | 禁用某知识后生成计划 | 不引用该知识 |
| R-010 | 参考知识返回 | 查看计划详情 | 显示参考知识标题 |

## 十、VIP 智能问答测试用例

| 用例编号 | 测试场景 | 操作 | 预期结果 |
| --- | --- | --- | --- |
| C-001 | 普通用户访问问答页 | USER 打开问答页 | 显示 VIP 专属提示 |
| C-002 | 普通用户调用问答接口 | USER 调用 /chat/ask | 返回 403 |
| C-003 | VIP 正常提问 | VIP 提交健康问题 | 返回回答 |
| C-004 | 问题为空 | question 为空 | 返回参数错误 |
| C-005 | 问题过长 | 超过 500 字 | 返回参数错误 |
| C-006 | 返回参考知识 | VIP 提问 | 返回 references |
| C-007 | 医疗风险提示 | 提问涉及疼痛或药物 | 提醒咨询医生 |
| C-008 | 非医疗声明 | 任意回答 | 末尾包含非医疗建议提示 |

## 十一、管理员测试用例

| 用例编号 | 测试场景 | 操作 | 预期结果 |
| --- | --- | --- | --- |
| M-001 | 管理员登录 | ADMIN 登录 | 登录成功 |
| M-002 | 查询用户列表 | GET /admin/users | 返回用户列表 |
| M-003 | 查询用户详情 | GET /admin/users/{id} | 返回用户详情 |
| M-004 | 禁用用户 | PUT /disable | 用户不能登录 |
| M-005 | 启用用户 | PUT /enable | 用户可登录 |
| M-006 | 查询用户体重 | GET /weights | 返回体重记录 |
| M-007 | 查询用户计划 | GET /plans | 返回计划记录 |
| M-008 | 普通用户访问后台 | USER 调用后台接口 | 返回 403 |

## 十二、前端页面测试用例

| 用例编号 | 页面 | 测试内容 | 预期结果 |
| --- | --- | --- | --- |
| F-001 | 登录页 | 输入账号密码登录 | 登录成功进入首页 |
| F-002 | 登录页 | 错误密码 | 显示错误提示 |
| F-003 | 注册页 | 注册新账号 | 注册成功 |
| F-004 | 首页 | 登录后进入 | 页面正常展示 |
| F-005 | 健康档案页 | 保存档案 | 保存成功 |
| F-006 | 体重记录页 | 添加体重 | 图表更新 |
| F-007 | AI 计划页 | 点击生成计划 | 显示 loading 和结果 |
| F-008 | 历史计划页 | 查看详情 | 详情正常显示 |
| F-009 | 智能问答页 | USER 访问 | 显示 VIP 提示 |
| F-010 | 智能问答页 | VIP 提问 | 显示 AI 回答 |
| F-011 | 管理员页 | ADMIN 访问后台 | 页面正常显示 |

## 十三、GSAP 动画测试

| 用例编号 | 测试场景 | 预期结果 |
| --- | --- | --- |
| G-001 | 登录页首次加载 | 表单和数据卡片依次进入 |
| G-002 | 心率曲线动画 | 曲线正常绘制 |
| G-003 | 数字递增动画 | 数字从 0 递增到目标值 |
| G-004 | 点击登录 | 按钮进入 loading 状态 |
| G-005 | 登录成功 | 页面过渡到首页 |
| G-006 | 登录失败 | 动画不影响错误提示 |

## 十四、安全测试

| 用例编号 | 测试场景 | 预期结果 |
| --- | --- | --- |
| S-001 | 数据库查看密码 | 密码为 BCrypt 密文 |
| S-002 | 前端搜索 API Key | 不存在 DeepSeek API Key |
| S-003 | 无 Token 请求接口 | 返回 401 |
| S-004 | 普通用户访问管理员接口 | 返回 403 |
| S-005 | 普通用户访问 VIP 问答 | 返回 403 |
| S-006 | 用户访问他人数据 | 返回 403 |
| S-007 | 日志检查 | 不输出密码、Token、API Key |

## 十五、数据库测试

| 用例编号 | 测试场景 | 预期结果 |
| --- | --- | --- |
| D-001 | Flyway 初始化 | 自动创建全部 MySQL 表 |
| D-002 | 用户名唯一 | 重复用户名插入失败 |
| D-003 | 健康档案唯一 | 一个用户只对应一份档案 |
| D-004 | 体重记录查询 | 可按用户和日期排序 |
| D-005 | 计划历史查询 | 可按创建时间倒序 |
| D-006 | 生成次数记录 | 每个用户每天一条记录 |

## 十六、异常测试

| 用例编号 | 测试场景 | 预期结果 |
| --- | --- | --- |
| E-001 | DeepSeek 调用失败 | 返回 AI 服务调用失败 |
| E-002 | Chroma 检索失败 | 返回 Chroma 检索失败 |
| E-003 | MySQL 连接失败 | 后端启动或接口返回异常 |
| E-004 | 参数类型错误 | 返回参数错误 |
| E-005 | 请求不存在数据 | 返回数据不存在 |

## 十七、Postman 测试建议

### 1. Collection 分组

```text
User
Profile
Weight
Plan
Chat
Admin User
Knowledge
```

### 2. 环境变量

| 变量名 | 说明 |
| --- | --- |
| baseUrl | http://localhost:8080/api/v1 |
| userToken | 普通用户 Token |
| vipToken | VIP 用户 Token |
| adminToken | 管理员 Token |
| userId | 测试用户 ID |
| planId | 测试计划 ID |
| knowledgeId | Chroma 知识 ID |

### 3. 基础断言

每个接口至少校验：

```javascript
pm.test("status code is 200", function () {
  pm.response.to.have.status(200);
});

pm.test("response has code", function () {
  const json = pm.response.json();
  pm.expect(json).to.have.property("code");
});
```

## 十八、缺陷记录模板

| 字段 | 内容 |
| --- | --- |
| 缺陷编号 | BUG-001 |
| 发现日期 | 2026-06-08 |
| 发现人 |  |
| 所属模块 |  |
| 严重程度 | 高 / 中 / 低 |
| 复现步骤 |  |
| 实际结果 |  |
| 预期结果 |  |
| 截图或日志 |  |
| 负责人 |  |
| 状态 | 待修复 / 修复中 / 已修复 / 已关闭 |

## 十九、测试通过标准

项目满足以下条件时可认为测试通过：

- 核心功能测试全部通过。
- 注册、登录、权限、AI 计划、VIP 问答、管理员后台无阻塞问题。
- 普通用户、VIP 用户、管理员权限隔离正确。
- 数据库中无明文密码。
- DeepSeek API Key 未暴露到前端。
- 关键接口 Postman 测试通过。
- 前端页面无明显报错。
- 严重缺陷数量为 0。
- 中等缺陷已修复或有明确说明。
