# API 补充说明

本文档记录相对 `接口文档.md` 的增量变更和修正。

## 一、新增端点

### 1.1 VIP 激活码系统

| 方法 | 端点 | 认证 | 说明 |
|------|------|------|------|
| POST | `/api/v1/admin/vip-codes/generate?count=N` | ADMIN | 生成 N 个激活码，默认 1 |
| GET | `/api/v1/admin/vip-codes/list` | ADMIN | 查看所有激活码 |
| POST | `/api/v1/user/activate-vip` | USER/VIP | 用户激活 VIP，需传 `{ "code": "VIP-XXXX" }` |

**激活码规则：**
- 格式 `VIP-XXXXXXXX`（8 位大写十六进制）
- 生成后 7 天过期
- 一次性使用
- 激活后用户获得 30 天 VIP 身份（`role=VIP`，`vip_expire_time` +30 天）

### 1.2 体重趋势

GET `/api/v1/weight/trend?period=week`

| 参数 | 必填 | 说明 |
|------|------|------|
| `period` | 否 | 可选 `week`/`month`/`year`，默认 `week` |

响应：
```json
{
  "dates": ["2026-06-01", "2026-06-02"],
  "values": [70.0, 69.5]
}
```

### 1.3 管理员额外接口

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/v1/admin/users/{id}/profile` | 查看用户健康档案 |
| DELETE | `/api/v1/admin/users/weights/{recordId}` | 管理员删除体重记录 |
| DELETE | `/api/v1/admin/users/plans/{planId}` | 管理员删除计划记录 |

### 1.4 AI 配置

| 方法 | 端点 | 认证 | 说明 |
|------|------|------|------|
| GET | `/api/v1/ai-config` | USER/VIP/ADMIN | 查看用户的 AI 配置 |
| POST | `/api/v1/ai-config` | USER/VIP/ADMIN | 保存/更新 AI 配置 |
| DELETE | `/api/v1/ai-config` | USER/VIP/ADMIN | 删除 AI 配置，恢复默认 |

**AI 路由规则：**

| Provider | 说明 | API Key 来源 | 模型 |
|----------|------|-------------|------|
| DEFAULT | 使用服务器默认 DeepSeek Key | 服务端 `.env` | `deepseek-chat` |
| CUSTOM | 使用用户自定义 API Key | 用户提交 | 用户指定（如 `deepseek-chat`）|
| LOCAL | 使用本地 Ollama 模型（需 VIP） | 无需 | 用户指定（如 `qwen3.5:9b`）|

**注意：**
- `POST /ai-config` 保存时会自动测试 API Key 连通性，失败返回 600
- API Key 在数据库中 AES 加密存储，响应中脱敏显示（`sk-ab****7890`）
- 删除配置后自动回退为 DEFAULT 模式

## 二、端点差异说明

### 2.1 注册接口

注册时 `role` 字段不可传入，系统默认分配 `USER`。  
需要升级为 VIP 需通过激活码激活。

### 2.2 用户当前信息

`GET /api/v1/user/current` 额外返回 `vipExpireTime` 字段：
```json
{
  "userId": 1,
  "username": "admin",
  "role": "ADMIN",
  "status": 1,
  "vipExpireTime": "2026-07-08T20:00:00"
}
```

## 三、安全配置

### CORS
已通过 `CorsFilter` 配置，限制为本地来源：
- `http://localhost:*`
- `http://127.0.0.1:*`

`SecurityConfig` 已配置 `.cors()` 以正确处理预检请求。

### 速率限制（Rate Limiting）

接口维度 IP 限流：

| 接口 | 限制 | 窗口 |
|------|------|------|
| `POST /api/v1/user/register` | 3 次 | 1 小时 |
| `POST /api/v1/user/login` | 5 次 | 1 分钟 |

超限返回 `429 Too Many Requests`，响应体：
```json
{
  "code": 429,
  "message": "请求过于频繁，请稍后重试",
  "data": null
}
```

## 四、环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DEEPSEEK_API_KEY` | 无（必填） | DeepSeek API 密钥 |
| `MYSQL_USERNAME` | `root` | MySQL 用户名 |
| `MYSQL_PASSWORD` | 无（必填） | MySQL 密码 |
| `JWT_SECRET` | 无（必填） | JWT 签名密钥 |
| `CHROMA_URL` | `http://localhost:8000` | Chroma 服务地址 |
| `OLLAMA_BASE_URL` | `http://localhost:11434` | Ollama 服务地址 |

启动前需执行 `export $(grep -v '^#' .env | xargs)` 加载 `.env` 文件，或直接设置环境变量。

## 五、文档修正记录

| 日期 | 文档 | 修正内容 |
|------|------|----------|
| 2026-06-08 | 接口文档.md §6.1 | `planId` → `id`（与代码统一） |
| 2026-06-08 | 接口文档.md §6.3 | 补充 `references` 字段 |
| 2026-06-08 | 数据库文档.md | 新增 `vip_activation_code` 表（九） |
| 2026-06-09 | 接口文档.md | 新增 AI Config 端点（§8）和 activate-vip / weight/trend |
| 2026-06-09 | 接口文档.md | 更新密码校验规则、daily limit 2→3、错误码 429 |
| 2026-06-09 | 接口补充说明.md | CORS 从 `*` 改为 `localhost:*`；新增速率限制说明 |
| 2026-06-09 | 接口补充说明.md | 环境变量移除默认值，新增 OLLAMA_BASE_URL、MYSQL_USERNAME |
| 2026-06-09 | 数据库文档.md | 新增 `user_ai_config` 表（十）、AI Provider 枚举 |
| 2026-06-09 | 数据库文档.md | 删除 V9 迁移记录（冗余），修正 code 长度 50→32 |
